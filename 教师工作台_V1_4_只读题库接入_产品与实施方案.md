# 教师工作台 V1.4「只读题库接入」产品与实施方案

**日期：** 2026-08-24

**基线：** `checkpoint-V1.3-pass`

**状态：** 产品负责人已确认，作为 V1.4 冻结产品基线

## 1. 目标

V1.4 把旧题库中的已审核题目作为教师工作台的一个原生入口接入，同时保持旧题库完全只读。

首版只完成：

1. 一次性导入旧题库中 `ready + reviewed` 的题目、来源、标签、难度和图片；
2. 按关键词、年级、年份、月份、题型、标签和难度组合筛选；
3. 默认使用完整页面浏览结果，不永久占用题目详情空间；
4. 点击题目后，宽窗口在右侧展开详情，窄窗口或长题在结果下方展开；
5. 搜索通过独立类型化接口提供，未来其他工作台入口可以复用；
6. 单题可以复制到素材库，或复制到某门课程的具体课次。

V1.4 不组卷、不建立试题篮、不做错题、推荐、成绩或 AI 自动处理。

## 2. 工作台集成

- 左侧主导航新增“题库”，与搜索、外部资料、素材库、学生、备课、设置并列；
- 题库页必须运行在现有 Electron 工作台壳内，不得替换 `App` 或形成独立网站；
- 默认状态只显示搜索、筛选和结果列表；
- 宽屏点击结果后打开右侧详情，列表仍可继续切题；
- 窄屏时详情自然落到结果下方；老师可主动收起详情；
- 答案和解析默认收起；
- “加入课程”使用临时选择弹窗选择课程和具体课次，不改变当前搜索位置。

## 3. 数据与导入边界

旧题库 `E:\Wss_Tiku` 始终只读。导出器只读取：

- `data/question_bank.db` 中 `status = ready AND review_status = reviewed` 的题目；
- 这些题目直接关联的试卷元数据、标签、难度和图片；
- `storage/assets` 中被这些题目引用的图片。

导入格式固定为自包含 SQLite 快照 `.tqbank`：

- `format_version = 1`；
- 题目身份使用旧库稳定 `source_uid`；
- 图片以 SHA-256 去重并存入快照；
- 导入先复制到工作区 staging，完整校验后原子替换当前快照；
- 导入失败继续使用上一个有效快照；
- 工作台只用 SQLite readonly + query-only 模式查询快照；
- 未来更新通过重新导出一个完整 `.tqbank` 并“更换题库”，V1.4 不做增量包合并或后台同步。

真实快照、旧库、认证信息、Key、日志、原始上传和未审核题目不得进入 Git。

## 4. 单题复制

- “导入素材库”生成工作台托管的独立 Markdown 文件；
- “加入课程”必须选择一个有效 course 下的具体 lesson，再生成独立 Markdown 文件；
- Markdown 保留题干、选项、答案、解析、标签、来源和题目图片；
- 写入使用临时文件与现有 `ManagedFileService` 的原子托管流程；
- 更换或删除工作台内的题库快照，不影响已复制的独立文件；
- 任何复制都不得回写旧题库。

## 5. 技术边界

- Renderer 只调用严格运行时校验的 Preload / IPC，不接触 SQLite、Node、任意路径或图片 BLOB；
- 搜索 SQL 只允许白名单字段且全部参数化；
- 单题详情限制图片总大小，异常包不能耗尽 Renderer 内存；
- 快照复制、Hash 和导出不在 Renderer 中执行；
- 继续复用 `NodeService`、`ManagedFileService`、工作区路径和现有导航，不改变 V1.1 备课、V1.2 课程进度或 V1.3 快速建课语义；
- 题库正文不会自动进入 AI Context。

## 6. 冻结的实施切片

1. `V14-01`：`.tqbank` 导出器、原子导入、只读查询服务、契约、Preload 和安全 IPC；
2. `V14-02`：工作台原生题库入口、默认浏览、右侧 / 下方详情、公式图片、答案解析和单题复制；
3. `V14-03`：全量测试、production build、真实快照 smoke、安全审计和代表性 Windows 流程。

## 7. 验收

1. 从旧库生成完整快照时，旧数据库和旧图片不发生变化；
2. 工作台能导入、替换、搜索、组合筛选和分页浏览；
3. 默认不显示永久详情区，点击后按窗口宽度在右侧或下方展开；
4. 题干、图片、选项、公式、来源、答案和解析可读；
5. 单题能复制到素材库和有效课次，快照更换后副本仍可打开；
6. 无效包不会替换当前有效包；
7. 全量测试、typecheck、lint、production build、真实快照 smoke 和 `git diff --check` 通过；
8. 不运行 portable / installer，不自动 push，不创建 `checkpoint-V1.4-pass`，直到产品负责人最终体验确认。
