# 教师工作台 V1.5「课程与课件导航重构」产品与实施方案

**日期：** 2026-08-25

**稳定基线：** `checkpoint-V1.4-pass`

**状态：** 产品负责人已确认方向，作为 V1.5 冻结产品基线

## 1. 问题

现有界面把“打开一节课”和“进入备课流程”混成了同一个动作：课程页双击课次会直接切到全局“备课”页面。教师只是想回看某节课的课件、资料或已保存成果时，会被迫进入一个任务流程，也失去当前课程浏览位置。

教师实际需要两条都自然成立的路径：

```text
课程 → 阶段 → 课次 → 浏览本课课件
学生 → 关联课程 → 阶段 → 课次 → 浏览本课课件
```

“浏览已有内容”是实体导航；“备一节课”是明确动作。两者必须分开。

## 2. 目标

V1.5 只重构课程、课次、课件与备课之间的 Renderer 信息架构：

1. 左侧“我的课程”改名为“课程”，成为课程、阶段、课次和本课课件的统一浏览入口；
2. 不新增与课程树并列的独立“课件”主导航，避免课程和课件形成两套归属；
3. 单击课次只选择 Viewed Lesson；双击课次或点击“查看课件”打开该课次的课件浏览区；
4. 只有明确点击“开始备课 / 继续备课”才进入全局备课工作台；
5. 课件浏览区允许在同一课程内快速切换上一课、下一课或指定课次，不必退回课程列表；
6. 学生页中的在读课程和历史课程都能进入对应课程，并可返回原学生；
7. 从课程课件进入备课后，返回课程时恢复原课程、原课次和课件浏览位置。

## 3. 信息架构

左侧主导航保持一个稳定的实体入口和一个任务入口：

```text
课程：查课程、查课次、翻课件、看课程学生
学生：查学生、看其在读/历史课程，再进入课程
备课：待处理草稿与 AI 备课工作台
素材库：跨课程复用的公共素材
```

课件只属于具体 lesson。跨课程复用的资料仍属于素材库；题库单题加入课程仍必须选择具体 lesson。V1.5 不复制课程树，不建立第二套课件目录，也不改变 managed 文件物理路径。

## 4. 课程与课次交互

### 4.1 课程入口

- 左侧主导航显示“课程”，替代“我的课程”；
- 课程列表、活动/结束筛选、快速建课、今日点名和 Current Lesson 逻辑保持不变；
- 课程卡正文负责选择课程；卡片上的“开始备课 / 继续备课”等明确动作仍可直接执行。

### 4.2 课次目录

- 课程详情的“课次”分区改为更明确的“课次目录”；
- 单击课次只更新 Viewed Lesson，不改变 Current Lesson，也不进入备课；
- 双击课次等价于点击可见的“查看课件”按钮，进入该 Viewed Lesson 的课件浏览区；
- 双击只能作为效率捷径，键盘和单击用户必须能通过“查看课件”完成同一操作；
- “开始备课 / 继续备课”保留为独立按钮，不复用课次行的打开行为。

### 4.3 本课课件

- 课程详情原“资料”分区改名为“课件”；
- 顶部明确显示 `课程 / 阶段 / 课次` 上下文；
- 提供上一课、下一课和按阶段分组的课次切换；切换只改变 Viewed Lesson；
- 复用现有 LessonMaterialReader，显示本课 Markdown、图片、普通文件和正文引用素材；
- 支持刷新、系统打开和定位文件；不编辑外部原件；
- 空课次明确显示“本课还没有课件”，并提供“开始备课 / 继续备课”入口；
- 已结束课程、学生已退出的历史课程仍可只读浏览课件。

## 5. 学生到课程

- 学生详情继续显示在读课程和历史课程；
- 课程行正文和“进入课程”动作都进入左侧“课程”，并选中对应课程；
- 从学生进入课程时记录来源学生，课程页提供“返回学生：姓名”；
- 从课程继续选择阶段、课次和课件，与直接从“课程”进入使用同一套页面和数据，不复制学生专属课程树；
- 返回学生后恢复原学生选择。

## 6. 备课边界与返回

- 全局“备课”仍是草稿箱和 AI 备课工作台，不承担课程浏览；
- 只有“开始备课 / 继续备课”、草稿箱草稿或其他明确备课按钮能进入备课；
- 从课程课件进入备课时，Renderer 保存返回目标：course、lesson、`courseware` 分区和可选来源学生；
- 点击“返回课程”恢复该目标，不退回默认 Current Lesson，也不改变 Current Lesson；
- 从全局草稿箱进入且没有课程浏览来源时，返回“课程”即可，不伪造来源位置。

## 7. 数据与技术边界

- 不新增 schema，不做 migration，不改变 course、period、lesson、course_students、course_progress、lesson_sessions、notes 或 files 的事实语义；
- Current Lesson 继续表示默认下一次处理位置；Viewed Lesson 只属于 Renderer；Prep Lesson 继续由 LessonPrepContext 决定；
- 导航目标使用类型化 Renderer 状态，不把临时浏览位置写入 SQLite；
- 复用 CourseDashboard、CourseDetail、StudentsPage、LessonFilesSection、LessonMaterialReader、DraftPanel 和现有服务；
- Renderer 仍只经类型化 Preload / IPC 使用文件和数据能力，不获得 Node、SQLite、任意路径或 Key；
- 不改变外部资料只读、managed 原子写入、AI 不覆盖原资料和备份边界。

## 8. 不做

V1.5 不加入独立课件库、第二棵课程树、文件拖拽重组、课程级共享文件、课件版本管理、多人协作、云同步、成绩分析、错题本、推荐、组卷、新 AI 工作流或数据模型重构。

## 9. 冻结的实施切片

1. `V15-01`：Renderer 课程导航目标、学生来源和备课返回语义；主导航“课程”改名；
2. `V15-02`：课次打开行为、课件浏览区、课次快速切换和完整双向入口；
3. `V15-03`：全量回归、production build、安全审计和代表性本地 Windows 流程。

## 10. 验收

1. 左侧只显示一个“课程”入口，不再显示“我的课程”，也不新增独立“课件”主导航；
2. 单击课次只选择，双击课次和“查看课件”都打开同一课件浏览区，且 Current Lesson 不变；
3. 课件区可切换上一课、下一课或指定课次，并正确刷新本课文件和上下文；
4. 进入课件不会进入备课；只有明确备课动作才进入备课工作台；
5. 从课件进入备课再返回，恢复原课程、原课次和课件区；
6. 从学生的在读/历史课程进入后可浏览课次课件，并能返回原学生；
7. 课程管理、Current Lesson、点名、确认已上、快速建课、题库加入课次、素材库、搜索和 V1.1 备课内核无回归；
8. 全量测试、typecheck、lint、production build、`git diff --check` 和代表性 Windows 流程通过；
9. 不运行 portable / installer，不自动 push；产品负责人最终体验确认前不创建 `checkpoint-V1.5-pass`。
