# 教师工作台 V1.2｜课程与学生信息架构重构：产品与实施方案

**状态：** V1.2 正式实施基线已冻结，待 V1.1 最终体验确认后实施
**版本：** V1.2
**日期：** 2026-08-23
**建议实施基线：** checkpoint-V1.1-pass
**产品定位：** 不重做 V1.1 备课内核，只整理课程、课次、学生、点名和实际教学进度。

---

# 1. 最终产品原则

V1.2 最重要的前提是：

> **课程结构是老师预先安排的教学计划，不是系统强制执行的流水线。**

> **系统负责给出最可能的下一步，老师负责确认真实发生了什么。**

学生可能：

- 从课程中间开始；
- 跳过某些课次；
- 调整原定顺序；
- 某个内容再上一遍；
- 上几次后退课；
- 只完成一个阶段；
- 暂停后再继续；
- 在班课中途加入或退出。

因此以下状态必须彼此独立：

~~~text
课次顺序
  = 原定计划

课次已上
  = 老师确认的实际教学事实

当前课次
  = 下一次默认要处理的课次

课程结束
  = 老师明确作出的课程生命周期决定

学生在读 / 已退出
  = 该学生与课程之间的有效关系
~~~

系统不得根据其中一个状态，擅自推导其他状态。

特别是：

~~~text
第 8 课已上
≠ 第 9 课一定要上
≠ 下一阶段一定开始
≠ 所有学生一定继续
~~~

---

# 2. 本次目标与范围

V1.2 只处理以下问题：

1. “我的课程”内容过多，课程、阶段、课次、学生、记录和资料堆在一起；
2. “学生”入口目前只是通用文件面板，不是真正的学生管理入口；
3. 缺少持久化的“下一次默认处理哪一课”；
4. 缺少轻量的课次日期、点名和“本课确实上过”记录；
5. 阶段结束、课程结束和学生退出没有明确语义。

本次交付：

1. 重排“我的课程”；
2. 拆出课程列表和课程详情；
3. 课程详情只保留课次、学生、资料；
4. 增加当前阶段与当前课次的轻量持久状态；
5. 增加“确认本课已上”和软推进；
6. 增加课程结束 / 重新开启；
7. 增加学生退出课程 / 重新加入；
8. 增加真正的学生列表和学生详情；
9. 增加可选课次时间和轻量点名；
10. 创建课程改为 Modal；
11. 原样接入 V1.1 备课流程。

本次明确不做：

- 排课日历；
- 重复排课规则；
- 今日课程独立页面；
- 系统提醒和消息通知；
- 迟到分钟、定位签到、补签审批；
- 出勤率和完整报表；
- 课程百分比；
- 学生画像和成绩分析；
- 学生文件入口；
- 学习记录附件；
- 复杂文件共享；
- 新 AI 工作流；
- Workflow / Agent；
- 草稿版本树；
- 多次上课复用同一个课次的复杂模型；
- 大规模数据库重构。

第一版仍采用：

> **一个 lesson 表示一次计划课次。**

如果同一内容需要再上一节，老师新建或复制一个课次即可，不在 V1.2 内增加多 session 模型。

---

# 3. 信息架构

主导航继续复用现有结构：

~~~text
我的课程
搜索
外部资料
素材库
学生
备课
设置
~~~

各页面只回答一个核心问题：

| 页面 | 核心问题 | 页面保留内容 |
|---|---|---|
| 我的课程 | 我要处理哪门课、哪一课？ | 今日待点名、课程、阶段、课次、实际状态、课次资料、关联学生入口 |
| 学生 | 这个学生长期学得怎么样？ | 在读 / 历史课程、人工学习记录 |
| 备课 | 这一课具体怎么准备？ | 本次资料、Skill、本次要求、AI 生成、编辑、保存 |

两条主线：

~~~text
课程 → 课次 → 备课

学生 → 关联课程 / 学习记录
~~~

两边只通过 ID 跳转，不复制两套页面。

---

# 4. 我的课程

## 4.1 页面结构

采用：

~~~text
全局导航 | 课程列表 | 课程详情
~~~

标题区域只保留：

~~~text
全部课程 4    待处理草稿 3
~~~

“待处理草稿”统计所有未删除、状态为 draft 的草稿，可进入现有草稿入口。它表示仍需继续处理的数据库草稿，不表示文件尚未落盘或可能丢失。课程卡不再显示“2 个草稿”之类容易混淆的数字。

## 4.2 今日待点名

课程页标题下增加一条轻量区域：

~~~text
今日待点名

今天 18:30
李四提升班 · 2026 秋季 · 第 8 课 整式加减
6 位学生                         待点名  [点名]
~~~

规则：

- 读取 scheduled_at 为本地今天的课次；
- 课程已经结束时不显示；
- 与 Current Lesson、Active Period 无关；
- 即使老师提前排好下一阶段，只要课程未结束，到日期仍应出现；
- 没有今日课次时整块隐藏；
- 保存点名后当天继续显示“已到 4 / 6 · 修改点名”；
- 没有关联的在读学生时显示“未关联学生”，禁用保存；
- 过去漏点名的课次不进入今日区域，从具体课次补录。

今日待点名只是快捷入口，不负责改变课程进度。

## 4.3 课程卡

课程卡只显示：

- 课程名称；
- 一对一 / 班课；
- 当前在读学生或人数；
- 当前阶段；
- 当前课次；
- 一个主按钮。

示例：

~~~text
张三一对一                              一对一
学生：张三
2026 秋季 · 当前第 8 课 有理数混合运算

[继续备课]
~~~

课程卡不显示：

- 百分比进度；
- 大量状态标签；
- 今日时间；
- 文件统计；
- 草稿数量；
- 成果数量；
- 无意义的 0 数据。

点击课程卡本身，在右侧打开详情。

## 4.4 课程卡主按钮

### 当前课次有待处理草稿

~~~text
[继续备课]
~~~

打开 Current Lesson 最近更新的待处理草稿。

### 当前课次没有待处理草稿

~~~text
[开始备课]
~~~

进入现有 V1.1 备课流程。

### 没有任何课次

~~~text
[创建第一课]
~~~

进入课程详情，先创建第一个阶段和第一课。

### 已有课次但尚未初始化 Current Lesson

~~~text
[选择下一课]
~~~

用于旧课程迁移，或课程已经有课次但尚未设置默认下一课的情况。

### 当前阶段已到边界或课程暂时不继续

~~~text
[决定下一步]
~~~

进入课程详情，由老师选择下一课、下一阶段或结束课程。

### 课程已结束

已结束课程不出现在默认活动列表，在“已结束”筛选中查看，可重新开启。

---

# 5. 课程详情

顶部显示：

~~~text
张三一对一
学生：张三
当前：2026 秋季 / 第 8 课 · 有理数混合运算
~~~

下方只保留：

~~~text
课次 | 学生 | 资料
~~~

默认打开“课次”。

## 5.1 课次

按照“阶段 → 课次”展示：

~~~text
2026 秋季                              + 新建课次

第 6 课  有理数的认识                 已上
第 7 课  有理数的运算                 已上 · 有成果
第 8 课  有理数混合运算               当前课次
第 9 课  一元一次方程应用
第 10 课 二次根式
~~~

“新建阶段”和“新建课次”使用局部按钮和小 Modal / Popover，不再长期显示输入框。

点击课次只改变 Viewed Lesson，不改变 Current Lesson。

## 5.2 “第 N 课”的编号

编号只用于显示，不新增 lesson_number 字段。

计算规则：

~~~text
同一阶段内
→ 按 lesson.sort_order 排序
→ 使用稳定 ID 作为相同 sort_order 的兜底
→ 从第 1 课开始显示
~~~

每个阶段重新从第 1 课开始：

~~~text
2026 秋季 / 第 12 课
2027 春季 / 第 1 课
~~~

调整排序后显示编号会变化，但 lesson ID、草稿、资料、点名和学习记录关系不变。

## 5.3 学生

课程详情中的“学生”只负责：

- 显示当前在读学生；
- 关联已有学生；
- 将学生标记为退出课程；
- 查看已退出学生；
- 重新加入；
- 点击进入学生详情。

不在课程首页填写长期学习记录。

## 5.4 资料

课程详情中的“资料”固定表示：

> 当前 Viewed Lesson 的 lesson_files。

它不是整门课程的全量资料，也不是学生文件。

没有 Viewed Lesson 时显示：

~~~text
请先选择一个课次查看资料。
~~~

---

# 6. 三种课次上下文

## 6.1 Current Lesson：当前课次

含义：

> 老师目前指定的“下一次默认要处理哪一课”。

它不是：

- 完整教学进度；
- 已完成课次的最大编号；
- 最近查看课次；
- 最近修改的草稿；
- 系统推测的唯一正确下一课。

特点：

- 每门活动课程最多一个；
- 持久化；
- 关闭应用后保留；
- 老师可以随时修改或清空。

## 6.2 Viewed Lesson：正在查看课次

含义：

> 老师此刻在课程详情中点开的课次。

特点：

- 只存在 Renderer；
- 不持久化；
- 打开课程时优先查看 Current Lesson；
- 点击其他课次不会改变 Current Lesson。

## 6.3 Prep Lesson：本次备课课次

含义：

> 实际进入 V1.1 备课工作台准备的课次。

它来自 LessonPrepContext.lessonId。

例如：

~~~text
Current Lesson：第 8 课
Viewed Lesson：第 9 课
点击第 9 课的“开始备课”

Prep Lesson：第 9 课
Current Lesson：仍为第 8 课
~~~

提前备课不会改变课程默认进度。

---

# 7. “本课已上”与软推进

## 7.1 正常流程

正常情况下保持一键操作：

~~~text
Current Lesson：第 8 课
→ 点名
→ 上课
→ 确认本课已上
→ 默认下一课：第 9 课
~~~

确认界面必须让老师看到系统准备采用的下一步：

~~~text
确认第 8 课已上

下一课：第 9 课 一元一次方程应用       [更改]

[取消] [确认]
~~~

老师不点击“更改”即可按正常顺序继续。

## 7.2 更改下一步

老师可在确认前选择：

- 下一课使用系统建议；
- 选择其他任意有效课次；
- 暂不设置下一课。

这覆盖：

- 跳课；
- 补课；
- 顺序调整；
- 暂停课程；
- 学生不按原计划学习。

确认过的 lesson 不允许继续作为 Current Lesson。

如果同一内容确实还要再上一节：

~~~text
创建或复制一个新课次
→ 标题可以与原课次相同
→ 将新课次设为 Current Lesson
~~~

如果当前这节课尚未结束，只保存点名，暂不执行“确认本课已上”。

## 7.3 默认建议规则

系统只给建议，不直接替老师决定：

1. 如果确认的课次就是 Current Lesson，默认建议同一阶段中排序靠后的下一节未确认课次；
2. 如果确认的是非 Current Lesson，默认保持原 Current Lesson 不变；
3. 如果同一阶段后面没有可建议课次，默认“暂不设置下一课”；
4. 不跨阶段自动推进；
5. 不因为新增阶段或课次自动改变 Current Lesson；
6. 已确认上过的课次不得被建议或手工设置为 Current Lesson。

Renderer 负责展示默认建议，Service 接收老师已经确认的明确决定；Service 不在后台偷偷猜测下一课。

## 7.4 实际上课记录

“确认本课已上”必须写入该 lesson 的 taught_confirmed_at。

因此：

~~~text
课次已上
  = taught_confirmed_at 非空

课次未确认已上
  = taught_confirmed_at 为空
~~~

任意有效课次都可以单独确认，不要求从第 1 课依次确认。

低频菜单提供“撤销本课已上”。撤销只清除 taught_confirmed_at，不自动回退 Current Lesson，避免二次猜测；如需修正默认课次，老师再使用“调整当前课次”。

重复确认必须幂等：

~~~text
taught_confirmed_at 为空
  → 首次写入并执行老师确认的 Current Lesson 决定

taught_confirmed_at 非空
  → 不覆盖原时间
  → 不再次执行 Current Lesson 决定
  → 返回 already_confirmed
~~~

如果老师确认错误，必须先“撤销本课已上”，再重新确认。

## 7.5 原子性

确认动作在一个 Main / Service 事务中完成：

~~~text
确认 lesson 仍有效
→ UPSERT lesson_sessions 扩展行
→ 仅在 taught_confirmed_at 为空时条件写入
→ 按老师确认的决定保持 / 清空 / 设置 Current Lesson
→ 提交
~~~

如果条件写入未影响任何行，事务不修改 Current Lesson，返回 already_confirmed。

重复点击不得覆盖时间或重复推进，过期页面不得覆盖较新的课程状态。

---

# 8. 阶段边界、课程结束和重新开启

## 8.1 到达阶段末尾不等于阶段已经完成

确认计划列表中的最后一课后，只表示已经到达当前阶段的计划末尾。

它不代表前面的所有课次都上过，也不代表这个阶段必须结束。老师仍然可以返回其他课次、补课或继续添加课次。

系统不自动进入下一阶段。

即使已经创建了 2027 春季，也停在边界：

~~~text
2026 秋季已到计划末尾

[开始 2027 春季]
[选择其他课次]
[暂不继续]
[结束课程]
~~~

如果还没有下一阶段：

~~~text
2026 秋季已到计划末尾

[创建下一阶段]
[选择其他课次]
[暂不继续]
[结束课程]
~~~

“开始下一阶段”表示老师明确决定切换教学阶段。确认后：

- 设置 active_period_id；
- 默认选择该阶段第一节有效、未确认课次；
- 允许老师在确认前改选其他课次。

V1.2 不增加 period_completed_at，也不根据“所有课次是否确认”计算阶段完成度。

## 8.2 课程可以在任何时候结束

老师可从课程低频菜单选择：

~~~text
结束课程
~~~

结束课程：

- 设置 course_progress.ended_at；
- 从活动课程列表隐藏；
- 不再出现在今日待点名；
- 不删除课程树、资料、草稿、点名、学习记录和学生关系历史；
- 不强制清空 active_period_id 或 current_lesson_id。

保留指针是为了重新开启时能继续原位置。

## 8.3 重新开启课程

“已结束”筛选中可以查看历史课程并选择：

~~~text
重新开启课程
~~~

重新开启只清除 ended_at：

- 如果原 Current Lesson 仍有效，继续使用；
- 如果指针失效或为空，由老师选择下一课；
- 不自动关联已经退出的学生。

## 8.4 阶段暂停

阶段边界选择“暂不继续”时：

- 课程仍然是活动课程；
- active_period_id 保留；
- current_lesson_id 为空；
- 课程卡显示“决定下一步”；
- 不等同于课程结束。

---

# 9. 学生加入、退出和历史关系

学生不一定从课程开始一直上到结束，因此不能删除 course_students 关系来表达退课。

## 9.1 在读

~~~text
course_students.ended_at IS NULL
~~~

表示学生当前仍在该课程中。

## 9.2 退出课程

老师可以在课程学生列表中选择“退出课程”：

- 设置 ended_at；
- 不再进入未来首次点名名单；
- 不删除历史点名；
- 不删除学生学习记录；
- 学生详情仍能看到这门历史课程，并标记“已退出”。

班课中一位学生退出，不影响其他学生和整门课程。

一对一学生退出后，如果课程没有在读学生，只提示老师是否结束课程，不自动结束。

## 9.3 重新加入

重新加入清除 ended_at：

- 恢复为在读；
- 进入之后首次创建的点名名单；
- 不修改过去课次的点名快照。

V1.2 不保存多次加入 / 退出的完整区间历史；如未来确有需要，再升级为独立 enrollment 表。

## 9.4 一对一限制

一对一课程最多只能有一位 ended_at 为空的学生。

历史上曾经关联过其他学生不影响新关联，但不能同时存在两位在读学生。

---

# 10. 课次日期与点名

## 10.1 课次日期

创建或编辑课次时增加：

~~~text
上课日期与时间（可选）
~~~

第一版一个 lesson 只保存一个 scheduled_at。

时间规则固定为：

~~~text
数据库
  = UTC ISO 8601
  = 例如 2026-08-23T10:30:00.000Z

Renderer
  = 按 Windows 系统本地时区显示

今日待点名
  = 先取得本地当天 00:00 到次日 00:00
  = 再转换成 UTC 起止范围查询或派生
~~~

不得存储不带时区含义的日期时间字符串，也不得用 UTC 自然日直接判断“今天”。

不支持：

- 每周重复；
- 批量排课；
- 节假日顺延；
- 拖拽日历；
- 系统通知。

没有日期的课次仍可备课、确认已上和手工点名，只是不进入今日待点名。

## 10.2 点名入口

只有两个：

~~~text
我的课程 → 今日待点名 → 点名

我的课程 → 课程详情 → 具体课次 → 点名 / 修改点名
~~~

不新增“考勤”导航。

## 10.3 点名弹窗

弹窗只显示：

~~~text
课程与课次
上课日期与时间
学生名单
全部到课
到课 / 请假 / 缺席
取消 / 保存点名
~~~

每位学生固定三种可保存状态：

~~~text
present = 到课
leave   = 请假
absent  = 缺席
~~~

“未确认”只存在于弹窗临时状态，不写数据库。保存前必须完成所有人的选择。

## 10.4 名单快照

首次打开未保存点名的课次：

- 使用当前 ended_at 为空的课程学生；
- 没有学生时禁用保存；
- 保存时一次性写入完整名单；
- 保存瞬间再次查询当前在读 student IDs；
- 如果与弹窗提交的 student IDs 不完全一致，拒绝保存并提示“课程学生名单已经变化，请重新打开点名”。

已经保存过点名：

- 使用 lesson_attendance 历史快照；
- 后来加入的学生不自动进入旧课次；
- 后来退出的学生不从旧课次消失；
- 老师可以重新打开修改。

V1.2 不保存学生姓名快照；学生改名后历史点名显示新姓名。

V1.2 明确接受以下限制：

> **课次第一次保存点名时，名单以保存瞬间的当前在读学生为准。**

如果某节历史课从未形成过点名快照，而学生已经退出课程，V1.2 不根据 scheduled_at、created_at 或 ended_at 还原过去某一时刻的精确课程名单，因此该学生不会自动出现在这节课的首次补点名名单中。

已经形成的 lesson_attendance 快照不受学生后来退出影响，仍可正常查看和修改。

如未来确认“学生退出后首次补录历史点名”是高频需求，再升级为保存完整加入 / 退出区间的 enrollment 模型；V1.2 不为这个边缘情况扩大数据结构。

## 10.5 点名与实际上课互不替代

~~~text
保存点名
  = 保存学生出勤情况

确认本课已上
  = 保存 taught_confirmed_at，并执行老师确认的下一步
~~~

两者互不自动触发：

- 保存点名不改变 Current Lesson；
- 确认本课已上不伪造点名；
- 修改历史点名不会再次推进；
- 没有点名也允许确认本课已上；
- 有点名也不代表本课一定已经完成。

## 10.6 点名和实际上课均不依赖前序课次

任何有效 lesson 都可以独立：

- 保存或修改点名；
- 确认本课已上；
- 撤销本课已上；
- 进入备课。

Service 不检查前一课是否已经点名、是否确认已上，也不要求课次按 sort_order 依次发生。

以下流程必须成立：

~~~text
Current Lesson 初始为第 8 课

第 8 课原计划先上，但临时没有上
→ 第 8 课不点名、不确认

第 9 课按计划正常上
→ 直接完成第 9 课点名
→ 可以确认第 9 课已上
→ 因为确认的是非 Current Lesson，Current Lesson 仍为第 8 课

之后补上第 8 课
→ 再进入第 8 课补录点名
→ 再确认第 8 课已上
→ 第 9 课已经确认，默认建议下一节尚未确认的课次，例如第 10 课
~~~

第 8 课和第 9 课的 scheduled_at、attendance、taught_confirmed_at 相互独立；上述操作不会自动改写彼此，也不会仅因编号顺序修改 Current Lesson。

---

# 11. 创建课程

右上角：

~~~text
+ 创建课程
~~~

居中 Modal 只填写：

~~~text
课程名称 *
课程类型 *
  ○ 一对一
  ○ 班课
关联已有学生（可选）

[取消] [创建课程]
~~~

创建课程和可选关联学生必须在同一个 Main / Service 事务中完成。

创建成功：

~~~text
关闭 Modal
→ 进入课程列表
→ 自动选中新课程
→ 右侧显示空课程详情
→ 创建第一个阶段
~~~

不在创建课程弹窗中同时创建完整阶段和课次。

新课程创建第一节课时，如果尚无进度记录，可默认将它设为 Current Lesson；以后新增课次或阶段不自动改变。

---

# 12. 学生页

“学生”改造成真正的 StudentsPage：

~~~text
全局导航 | 学生列表 | 学生详情
~~~

## 12.1 学生列表

只显示：

- 学生姓名；
- 当前在读课程；
- 最近一条 manual 学习记录。

提供：

~~~text
搜索学生
+ 新建学生
~~~

不显示年龄、性别、家长电话、年级、画像、成绩、文件数量或大量派生统计。

## 12.2 学生详情

第一版不做复杂 Tab，只纵向显示：

~~~text
关联课程
最近学习记录
~~~

关联课程分为：

- 在读课程；
- 已退出 / 已结束的历史课程。

课程行提供“进入课程”，只做页面跳转。

学习记录只显示：

~~~text
note_kind = manual
~~~

明确排除 lecture、example、homework。

新增记录只填写：

~~~text
记录内容 *
关联课次（可选）
~~~

Service 必须验证被关联课次所属课程与该学生存在当前或历史关系。

---

# 13. 文件边界

## 13.1 课次资料

课次资料继续使用 lesson_files：

- 讲义；
- PPT；
- 例题；
- 练习；
- V1.1 保存成果。

课程“资料”页只显示 Viewed Lesson 的 lesson_files。

## 13.2 学生文件

V1.2 前端不提供学生文件入口。

原因是它会产生第二套文件管理判断：

~~~text
这份试卷应该放学生文件，还是放课次资料？
~~~

因此：

- 学生详情不显示学生文件；
- 不显示添加文件；
- 不显示文件数量；
- 不新增学习记录附件；
- 不开发 StudentFilesSection；
- 全局 managed 文件面板不再暴露新的学生目标操作。

已有 student_files 表、copyToStudent() 和历史数据继续保留：

- 不删除；
- 不迁移；
- 不破坏搜索、备份和恢复；
- V1.2 UI 暂不暴露入口。

---

# 14. V1.1 备课流程保持不变

点击“开始备课 / 继续备课”后继续进入现有 V1.1：

~~~text
本次备课课次
→ 本次资料
→ 外部资料 / 素材库
→ Skill（可选）
→ 本次要求（可选）
→ 生成讲义 / 例题 / 作业
→ 预览 / 编辑
→ 保存修改
→ 保存到本次课次
~~~

不重写：

- LessonPrepContext；
- ContextBuilder；
- AI Gateway；
- DraftService；
- DraftPanel 核心逻辑；
- Skill；
- 外部资料；
- 素材库；
- Parser；
- Search；
- Backup / Restore。

草稿规则：

- 全局“待处理草稿”统计所有未删除 draft；
- 课程卡只判断 Current Lesson 是否有草稿；
- 有则“继续备课”，打开最近更新的一条；
- 没有则“开始备课”；
- 其他课次草稿从全局草稿入口或对应课次进入；
- 课程卡不展示数字草稿统计。

---

# 15. 后端复用与最小数据增量

## 15.1 继续复用

| 现有模块 / 数据 | V1.2 处理方式 |
|---|---|
| nodes、NodeService | 继续负责 course / period / lesson、sort_order、移动和软删除 |
| students | 继续作为学生实体，只补独立创建入口 |
| course_students | 继续作为课程学生关系，新增 ended_at |
| notes | 继续保存 manual 学习记录和 V1.1 AI 结果 |
| lesson_files | 继续作为课次资料唯一关系 |
| student_files | 保留兼容，不提供 V1.2 UI |
| ManagedFileService | 继续复用，不重写 |
| DraftService / DraftPanel | 原样接入新的课程入口 |
| LessonPrepContext | 继续作为 Prep Lesson 真相 |
| Search / Parser / AI / Backup | 不改变产品语义，只做回归 |

## 15.2 Schema v12

建议只新增 / 调整以下数据：

~~~sql
ALTER TABLE course_students
  ADD COLUMN ended_at TEXT;

CREATE INDEX idx_course_students_active
  ON course_students(course_id, ended_at);

CREATE TABLE course_progress (
  course_id TEXT PRIMARY KEY NOT NULL
    REFERENCES nodes(id) ON DELETE CASCADE,
  active_period_id TEXT
    REFERENCES nodes(id) ON DELETE SET NULL,
  current_lesson_id TEXT
    REFERENCES nodes(id) ON DELETE SET NULL,
  ended_at TEXT,
  updated_at TEXT NOT NULL
);

CREATE TABLE lesson_sessions (
  lesson_id TEXT PRIMARY KEY NOT NULL
    REFERENCES nodes(id) ON DELETE CASCADE,
  scheduled_at TEXT,
  taught_confirmed_at TEXT,
  attendance_recorded_at TEXT,
  updated_at TEXT NOT NULL
);

CREATE INDEX idx_lesson_sessions_schedule
  ON lesson_sessions(scheduled_at);

CREATE TABLE lesson_attendance (
  lesson_id TEXT NOT NULL
    REFERENCES lesson_sessions(lesson_id) ON DELETE CASCADE,
  student_id TEXT NOT NULL
    REFERENCES students(id) ON DELETE RESTRICT,
  status TEXT NOT NULL
    CHECK (status IN ('present', 'leave', 'absent')),
  updated_at TEXT NOT NULL,
  PRIMARY KEY (lesson_id, student_id)
);
~~~

lesson_sessions 的语义固定为：

> **lesson 的时间、实际上课确认和点名扩展状态。**

它不表示“只有排过时间的 lesson”。设置时间、确认本课已上或保存点名时，都必须按需 UPSERT 这一行，scheduled_at 可以为 NULL。

## 15.3 状态语义

~~~text
没有 course_progress 行
  = 旧课程尚未初始化

ended_at 非空
  = 课程已结束

active_period_id 非空，current_lesson_id 非空，ended_at 为空
  = 活动课程有默认下一课

active_period_id 非空，current_lesson_id 为空，ended_at 为空
  = 阶段边界或暂停，等待老师决定

taught_confirmed_at 非空
  = 老师已确认该课次上过

attendance_recorded_at 非空
  = 该课次已经保存点名快照
~~~

数据库 FK 只能保证引用存在。Service 还必须验证：

1. course、period、lesson 类型正确且未软删除；
2. period 属于 course；
3. lesson 属于 period；
4. current_lesson_id 属于 active_period_id；
5. 不允许跨课程设置进度；
6. 节点移动或软删除后不能保留失效指针；
7. ended course 不接受普通推进，必须先重新开启。

旧 course_students 行迁移后 ended_at 默认为 NULL，不丢失现有关系。

---

# 16. Service 与 IPC 增量

## 16.1 读取结构

~~~ts
interface CourseProgressRecord {
  readonly courseId: string
  readonly activePeriodId: string | null
  readonly currentLessonId: string | null
  readonly endedAt: string | null
  readonly updatedAt: string
}

type AttendanceStatus = 'present' | 'leave' | 'absent'

interface LessonSessionSummary {
  readonly lessonId: string
  readonly scheduledAt: string | null
  readonly taughtConfirmedAt: string | null
  readonly attendanceRecordedAt: string | null
  readonly presentCount: number
  readonly leaveCount: number
  readonly absentCount: number
  readonly totalCount: number
}
~~~

加入：

~~~text
CoreOverview.courseProgress
CoreOverview.lessonSessions
~~~

课程列表通过一次 getOverview() 派生，避免逐行 IPC。

## 16.2 课程进度服务

最小接口：

~~~ts
type CurrentLessonDecision =
  | { readonly type: 'keep' }
  | { readonly type: 'clear' }
  | { readonly type: 'set'; readonly lessonId: string }

type ConfirmLessonResult =
  | {
      readonly status: 'confirmed'
      readonly lessonId: string
      readonly taughtConfirmedAt: string
      readonly progress: CourseProgressRecord
    }
  | {
      readonly status: 'already_confirmed'
      readonly lessonId: string
      readonly taughtConfirmedAt: string
      readonly progress: CourseProgressRecord
    }

confirmLessonTaught(input: {
  readonly courseId: string
  readonly lessonId: string
  readonly expectedCurrentLessonId: string | null
  readonly decision: CurrentLessonDecision
}): ConfirmLessonResult

undoLessonTaught(courseId: string, lessonId: string): void

setCurrentLesson(input: {
  readonly courseId: string
  readonly lessonId: string
  readonly expectedCurrentLessonId: string | null
}): CourseProgressRecord

clearCurrentLesson(input: {
  readonly courseId: string
  readonly expectedCurrentLessonId: string | null
}): CourseProgressRecord

startPeriod(
  courseId: string,
  periodId: string,
  initialLessonId: string,
): CourseProgressRecord

endCourse(courseId: string): CourseProgressRecord

reopenCourse(courseId: string): CourseProgressRecord
~~~

约束：

- confirmLessonTaught() 在单一事务内写实际上课确认和明确的进度决定；
- expectedCurrentLessonId 防止过期页面覆盖新状态；
- decision=set 时同步更新 active_period_id；
- decision=clear 时只清空 current_lesson_id，保持原有 active_period_id 不变；
- decision=keep 时不修改进度，但只允许在确认的 lesson 不是 Current Lesson 时使用；
- 被确认的 lesson 已有 taught_confirmed_at 时返回 already_confirmed，不覆盖时间，也不执行 decision；
- setCurrentLesson()、startPeriod() 和 decision=set 的目标都必须尚未确认已上；
- 不允许把刚刚确认的同一个 lesson 重新设为 Current Lesson；
- clearCurrentLesson() 为“调整当前课次 → 暂不设置下一课”提供独立能力；
- setCurrentLesson() 与 clearCurrentLesson() 都必须校验 expectedCurrentLessonId；
- undoLessonTaught() 不自动回退进度；
- startPeriod() 不默认强制第一课，Renderer 可预选第一课但老师能改；
- 只有 startPeriod()，或老师明确 decision=set 到其他阶段的课次时，才允许切换 active_period_id；
- endCourse() 只写 ended_at；
- reopenCourse() 只清除 ended_at。

不再实现“跨整个课程树自动寻找下一课”的 advanceCurrentLesson()。

## 16.3 课程学生关系

在现有 CoreDataService 上增量增加：

~~~ts
createStudent(name: string)
createCourse(input: {
  readonly name: string
  readonly mode: 'one_to_one' | 'class'
  readonly studentIds?: readonly string[]
})
linkStudentToCourse(courseId: string, studentId: string)
endCourseStudentLink(courseId: string, studentId: string)
reactivateCourseStudentLink(courseId: string, studentId: string)
~~~

不再用物理删除关系表达退课。

## 16.4 点名服务

保持一个很薄的 AttendanceService：

~~~ts
updateLessonSchedule(lessonId, scheduledAt)
getLessonAttendance(lessonId)
saveLessonAttendance(lessonId, entries)
~~~

只提供三个严格白名单 IPC：

~~~text
attendance:update-schedule
attendance:get-lesson
attendance:save-lesson
~~~

保存点名使用整份替换事务：

~~~text
校验 lesson 和名单
→ UPSERT lesson_sessions
  scheduled_at 可以为 NULL
→ 如果尚无历史点名快照
  再次查询当前在读 student IDs 并与提交集合严格比较
→ 如果已有历史点名快照
  与历史 student IDs 严格比较
→ 删除该 lesson 旧 attendance rows
→ 插入完整名单
→ 更新 attendance_recorded_at / updated_at
→ 提交
~~~

名单不一致时整个事务回滚，并返回“课程学生名单已经变化，请重新打开点名”。

Renderer 不直接接触 SQLite、Node、任意文件系统或秘密。

---

# 17. Renderer 状态边界

App.tsx 只提升跨页面导航目标：

~~~ts
selectedCourseId?: string
selectedStudentId?: string
~~~

页面内部状态留在页面：

~~~text
viewedLessonId
activeCourseTab
createCourseModalOpen
createPeriodModalOpen
createLessonModalOpen
createStudentModalOpen
learningRecordModalOpen
attendanceModalLessonId
confirmTaughtModalLessonId
progressMenuOpen
~~~

建议最小拆分：

~~~text
src/renderer/
  course-dashboard.tsx
  course-list.tsx
  course-detail.tsx
  course-lessons-section.tsx
  course-students-section.tsx
  lesson-files-section.tsx
  lesson-attendance-modal.tsx
  confirm-lesson-taught-modal.tsx

  students-page.tsx
  student-list.tsx
  student-detail.tsx
~~~

不为追求文件数量一次拆出过多组件；最低要求是 CourseDashboard 不再承担所有录入、学生记录和文件职责。

---

# 18. 实施任务链

仍拆成五个小任务，只在 V1.1 最终验收后开始。

~~~text
V12-01 Core 与持久化
→ V12-02 课程页与点名交互
→ V12-03 学生页
→ V12-04 V1.1 备课与资料接入
→ V12-05 全量回归与版本验收
~~~

实施规则：

- 同一时刻最多一个 V12-xx 为 IN_PROGRESS；
- V12-01～V12-04 各自运行相关测试、typecheck 和 lint；
- 每个节点验收通过后更新 STATUS.md、GOAL_PROGRESS.md，并按版本控制协议创建本地里程碑提交；
- V12-05 才运行全量测试、production build 和代表性本地 Windows smoke；
- V1.2 及之后的小版本默认不运行 portable / installer packaging，不生成对外交付包；
- 只有产品负责人明确提出发布或打包要求时，才单独执行 packaging；
- 未经最终产品体验确认，不创建 checkpoint-V1.2-pass。

## V12-01｜Schema、课程生命周期、点名与 Core 增量

内容：

- schema v12；
- course_students.ended_at；
- course_progress；
- lesson_sessions / lesson_attendance；
- CoreOverview 增量；
- UTC ISO 8601 时间存储与本地日期范围派生；
- lesson_sessions 按需 UPSERT；
- confirmLessonTaught()、already_confirmed 与撤销；
- setCurrentLesson() / clearCurrentLesson() / startPeriod()；
- Current Lesson 目标必须是未确认课次；
- endCourse() / reopenCourse()；
- 学生退出 / 重新加入；
- 一对一在读学生限制；
- createCourse({ studentIds }) 与关联学生同事务；
- AttendanceService 与三个 IPC；
- 首次点名保存时重新校验当前在读学生集合；
- 历史点名严格匹配已有快照；
- 节点移动 / 软删除后的进度合法性处理。

验收：

- 旧数据无损迁移；
- 不允许跨课程设置；
- 确认任意课次都能独立记录；
- 重复确认返回 already_confirmed，不覆盖时间、不执行第二次进度决定；
- 手工清空 Current Lesson 有独立接口和并发校验；
- 无 scheduled_at 的 lesson 可以直接点名和确认已上；
- 第 8 课没有点名时，第 9 课仍可正常点名；
- 默认建议不在 Service 内偷偷执行；
- 阶段最后一课不会跨阶段推进；
- 结束和重新开启不丢历史；
- 学生退出不删除点名和学习记录；
- 首次点名保存前名单变化会被拒绝；
- 保存点名不改变 Current Lesson。

## V12-02｜我的课程、软推进与点名入口

内容：

- 课程列表和搜索；
- 活动 / 已结束筛选；
- 全局待处理草稿入口；
- 今日待点名；
- 创建课程 Modal；
- 空课程“创建第一课”；
- 有课次但无 Current Lesson 时“选择下一课”；
- 课程详情三块；
- 阶段和课次；
- 阶段内显示编号；
- Current / Viewed Lesson 分离；
- 确认本课已上 Modal；
- 下一课默认建议、选择其他课次和暂不设置；
- 不提供“继续停留在已确认课次”；
- 重复确认显示已确认结果，不覆盖时间；
- 阶段边界操作；
- 课程结束 / 重新开启；
- 日期时间和点名 Modal。

主验收：

~~~text
当前第 8 课
→ 确认第 8 课已上
→ 默认建议第 9 课
→ 老师直接确认
→ Current Lesson 成为第 9 课

当前第 8 课
→ 查看并确认第 10 课已上
→ 默认保持 Current Lesson 为第 8 课

第 8 课临时未上、未点名
→ 直接打开第 9 课点名并保存
→ 第 9 课可以独立确认已上
→ 之后回到第 8 课补录点名并确认
→ 两个课次记录互不覆盖

确认第 8 课已上
→ 再次点击确认
→ 返回 already_confirmed
→ 原确认时间和 Current Lesson 均不变化

确认阶段最后一课
→ 不进入下一阶段
→ 老师选择开始下一阶段 / 暂停 / 结束课程
~~~

## V12-03｜真正的学生页

内容：

- StudentsPage；
- 学生列表和搜索；
- 新建学生；
- 学生详情；
- 在读 / 历史课程；
- 进入课程；
- 最近 manual 学习记录；
- 添加记录；
- 可选关联课次；
- 课程进入学生详情。

明确不做学生文件、附件和 AI 结果混入人工记录。

## V12-04｜课次资料与 V1.1 接入

内容：

- 资料只显示 Viewed Lesson 的 lesson_files；
- 无 Viewed Lesson 时明确空状态；
- 开始 / 继续备课；
- Prep Lesson 文案；
- 保存到本次课次文案；
- 保持 V1.1 外部资料、素材、Skill、AI、草稿和编辑流程不变；
- student_files 数据、搜索、备份和恢复保持兼容；
- 收掉新的学生文件前端入口。

## V12-05｜全量回归与版本验收

自动检查：

~~~text
npm test
npm run typecheck
npm run lint
npm run build
git diff --check
~~~

npm run build 只用于 production 编译和打包器校验，不生成 portable 或 installer 交付物。

代表性流程：

~~~text
新建学生和班课
→ 创建 2026 秋季及若干课次
→ 第一课成为默认 Current Lesson
→ 为整学期部分课次设置日期
→ 今日课次出现在待点名
→ 确认日期跨本地午夜时仍归属正确日期
→ 保存并修改点名
→ 点名不推进课程
→ 打开首次点名后修改课程学生名单
→ 旧弹窗保存被拒绝并要求重新打开
→ 第 8 课临时未上且不点名
→ 第 9 课先正常点名并确认已上
→ 第 8 课之后再补录点名并确认
→ 提前备后面的课
→ Current Lesson 不变
→ 确认非 Current Lesson 已上
→ Current Lesson 默认保持
→ 确认 Current Lesson 已上
→ 接受系统建议的下一课
→ Current Lesson 更新
→ 确认阶段最后一课
→ 不跨阶段
→ 暂不继续后关闭并重开
→ 边界状态仍存在
→ 手工开始下一阶段
→ 一位班课学生退出
→ 历史点名仍保留
→ 结束课程并重新开启
→ 课程树、资料、草稿、记录均未丢失
~~~

只有最终产品体验确认后才创建：

~~~text
checkpoint-V1.2-pass
~~~

---

# 19. 最终验收标准

## 课程

老师打开“我的课程”后能立即知道：

~~~text
今天哪些课需要点名
当前默认处理哪一课
哪些课已经确认上过
下一步是备课、决定下一课，还是结束课程
~~~

## 进度

正常流程只需确认一次，系统预选最可能的下一课；例外情况下老师能在同一确认界面选择其他课次或暂不设置下一课。

已经确认上过的 lesson 不再作为 Current Lesson；需要同一内容再上一节时创建或复制新课次。

系统不强制从头到尾，不要求前序课次先发生，也不跨阶段自动推进。

## 点名

老师能从今日入口快速点名，也能从任意历史课次重新修改。点名与“本课已上”独立，任意课次的点名都不依赖前序课次。

## 学生

学生可以中途加入、退出和重新加入；历史课程、点名和人工学习记录不会因为退出而消失。

## 备课

老师可以准备任意课次，不会因此修改 Current Lesson；V1.1 主流程保持完整。

## 数据

所有新增写操作都经过 Main / Service 和严格 IPC；Renderer 不直接访问 SQLite、Node、文件系统或秘密。

---

# 20. 一句话结论

> **课程树负责表达计划，实际上课和点名负责表达事实，Current Lesson 只提供默认下一步，课程是否继续以及学生是否继续始终由老师决定。**

以上文字规格优先于任何参考图中的示例顺序、示例编号和旧交互。
