# 教师工作台 V1.1 回归与 Windows 交付验收

日期：2026-08-22，最终确认更新于 2026-08-23
状态：PASS；自动质量门、Windows 代表性流程和产品负责人最终体验均已通过。

## 交付候选

- 交付形式：Windows unpacked portable 目录，不增加安装器、签名、自动更新或卸载器。
- 可执行文件：`release-l12/win-unpacked/教师工作台.exe`。
- 工具链：Electron `43.4.1`、electron-builder `26.15.3`、Windows build `26200`。
- 最终可执行文件大小：`235,534,336` bytes；`resources/app.asar` 大小：`107,322,289` bytes。
- 工作区继续位于 portable 目录之外；重新打包或删除 portable 目录不会以工作区为删除目标。

## V1.1 主流程

新增 `tests/v1.1-acceptance.test.ts`，在一个隔离临时工作区中使用真实 Main service、SQLite schema v11、最小有效 DOCX/PPTX/PDF 和本地 fake provider，完成以下一条完整流程：

```text
创建班课和课次（不绑定学生）
→ 登记只读外部资料 root
→ 从外部资料加入 DOCX / PPTX / PDF
→ 从素材库加入一份 Markdown 资料
→ 解析并建立搜索索引
→ 选择 Skill 并填写本次要求
→ 生成讲义 → 原地修改 → 保存到课次
→ 重新生成并保留旧结果
→ 生成例题和作业并保存到课次
→ 关闭并重新打开工作区
→ 核对未保存草稿、已保存课次成果和修改正文仍然存在
```

结果：PASS。测试同时确认四份资料均为本课独立 managed 副本，外部原件和素材库原件内容未改变；Prompt 包含选定 Skill、本次要求、课次信息和明确选择的资料。

## V1 轻量回归

| 能力 | 证据 | 结果 |
|---|---|---|
| 课程、学生、阶段、课次与普通记录 | `tests/core-data.test.ts`、`tests/phase1-acceptance.test.ts` | PASS |
| managed 文件、素材复制、打开、删除与恢复 | `tests/managed-file-service.test.ts`、`tests/phase1-acceptance.test.ts` | PASS |
| 中文/数学搜索与索引重建 | `tests/phase2-acceptance.test.ts`、`tests/search-service.test.ts` | PASS |
| DOCX/PPTX/PDF/XLSX Parser 与顺序 Worker | `tests/document-parser.test.ts` | PASS |
| AI Gateway、三类生成、失败/取消边界 | `tests/ai-gateway.test.ts`、`tests/phase3-acceptance.test.ts` | PASS |
| 备份到新目录、恢复和搜索重建 | `tests/backup-restore.test.ts` | PASS |
| V1.1 Skill、课次选材、草稿箱、同行保存、重新生成 | `tests/v1.1-acceptance.test.ts` 及 V11-01–V11-04 专项测试 | PASS |

全量测试结果：`31` 个测试文件、`96` 项测试全部通过。

## Windows packaged smoke

使用最终 `release-l12/win-unpacked/教师工作台.exe`，通过独立的临时 app-data、Electron `user-data-dir`、假课程、假资料和本机 `127.0.0.1` fake AI 服务进行验证；未接触正式工作区、真实教学资料或真实 API Key。

- 最终 exe 在当前 Windows 机器上成功启动并暴露唯一窗口 `教师工作台`。
- UI 显示 `工作区已连接 · schema v11`，能读取隔离课程、课次和素材记录。
- 设置页显示两套预置模板：`AMC8 一对一常规备课`、`初中数学常规备课`。
- 在隔离 `user-data-dir` 中保存 fake model、localhost Endpoint 和 fake Key，连接测试返回 `连接成功 · v11-05-fake-model · 31 ms`。
- 在长页面滚动验证中，最左侧全局导航仍存在于固定导航区域，右侧内容独立滚动。
- 临时 fake Key 的明文负向扫描在隔离工作区、最终 portable 目录和 production build 中均为零命中。

Windows 前台当时有其他全屏应用持续最小化验收窗口，因此没有用桌面自动化重复一遍已由端到端测试覆盖的全部点击。按 V11-05 规则，最终放行仍需要产品负责人亲自在候选包中完成下一节所列的一次真实但不敏感流程；该项未完成前不创建最终通过标签。

## 安全与包内容审计

- 外部资料路径穿越、绝对路径、过期 root 和根外链接继续由 Main 拒绝。
- 外部原件与素材库原件不被课次副本、AI 草稿或人工编辑覆盖。
- Renderer 仍只通过类型化 Preload API 工作，不直接访问 Node、SQLite、任意文件系统或 Key。
- `contextIsolation: true`、`nodeIntegration: false`、`sandbox: true` 保持不变；未使用 `--no-sandbox`。
- `app.asar` 审计共 `996` 个文件；未发现 `.env`、运行数据库、SQLite 索引、日志、证书、Key、秘密、备份或工作区数据。
- portable/build 输出被 `.gitignore` 排除，不进入提交。

## 测试后修复候选重验（2026-08-23）

在 `fix(V1.1): repair local dev startup`（`16aff174298b49a14acfd60d3954931bd9019b53`）之后，针对当前 HEAD 重新执行最终门禁，结果如下：

- `npm test`：PASS，`32` 个测试文件 / `101` 项测试；
- `npm run typecheck`：PASS；
- `npm run lint`：PASS；
- `npm run build`：PASS；
- `npm run package:portable`：PASS，重新生成当前修复后的 `release-l12/win-unpacked`；
- `git diff --check`：PASS；
- 当前 exe 为 `235,534,336` bytes，`resources/app.asar` 为 `107,328,552` bytes；
- `app.asar` 仍为 `996` 个文件，文件名审计未发现 `.env`、数据库、索引、日志、证书、Key、备份或工作区数据；
- Windows packaged smoke 使用仓库内被忽略的隔离 app-data 与 `user-data-dir` 启动：唯一窗口标题为“教师工作台”，默认菜单隐藏，主工作区占满导航右侧空间，内容滚动后 104px 全局导航保持固定；随后使用正常窗口关闭，目标窗口数归零；
- 隔离工作区首次启动生成 schema v11，SQLite `integrity_check=ok`；没有读取正式工作区、真实教学资料或真实 API Key。

本次重验补齐了 V11-05 后两次修复对应的 build、portable 与 Windows 启动证据，不改变下一节的产品负责人体验确认要求，也不提前创建最终通过标签。

## 产品负责人最终确认（PASS）

2026-08-23，产品负责人明确回复“V1.1 最终体验通过”。该确认与上文当前候选重验共同满足 V1.1 最终门禁，可以在包含本确认记录的最终提交上创建本地 `checkpoint-V1.1-pass`；不 push。

请使用真实但不敏感的资料完成一次：

```text
我的课程 → 选择课次 → 开始备课
→ 从外部资料加入 PPTX / DOCX / PDF
→ 从素材库加入一份资料
→ 选择 Skill → 填写本次要求
→ 生成讲义 → 预览 → 编辑 → 保存修改
→ 重新生成并确认旧稿仍在
→ 生成例题和作业 → 保存到当前课次
→ 退出并重新打开 → 确认草稿和课次成果仍可用
```

确认口径：主流程可自然完成、正文只有一个预览/编辑区域、重新生成不覆盖旧稿、保存到课次后能再次打开。V11-05 的实现、交付里程碑和产品体验均已完成。

## 自动质量门

```text
npm test                 # PASS，31 files / 96 tests
npm run typecheck        # PASS
npm run lint             # PASS
npm run build            # PASS
npm run package:portable # PASS，electron-builder dir target
git diff --check         # PASS
```

## 已知限制 / Later

- 当前交付仍是未签名 portable 目录，不含安装器、卸载器、自动更新和升级矩阵。
- Office/PDF 高保真内嵌预览、OCR、外部目录实时监听、多个外部 root、向量搜索、流式 AI、工作流/Agent、草稿版本树、复杂 Skill 参数和企业级压力/故障矩阵不属于 V1.1。
- 真实付费 provider smoke 不是放行条件；最终体验可以使用用户现有可用 provider，但不得把 Key、真实资料或运行数据库加入 Git。
