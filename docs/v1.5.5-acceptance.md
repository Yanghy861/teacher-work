# 教师工作台 V1.5.5 验收记录（正确性与健壮性加固）

**日期：** 2026-08-31  
**基线：** `checkpoint-V1.5.4-pass`  
**当前状态：** V155-A–V155-E `DONE`（2026-09-01 产品负责人最终确认通过；`checkpoint-V1.5.5-pass` 创建于最终确认提交）

## 实施内容

| 任务 | 内容 | 结果 |
|---|---|---|
| V155-A | AI 修改范围元数据结构化：`drafts:generate` 载荷新增可选键 `modification`（`DraftModificationScope`），提示词标记串与 `DRAFT_PROMPT_VERSION` 不变，UI 还原优先读结构化键、旧笔记回退标记解析，`draft-scope.ts` 纯模块抽取 | DONE，相关 30 tests |
| V155-B | `material-library-ipc.test.ts` 补齐（与其余 11 个 IPC 模块同等强度）；`getOverview` 恒真 WHERE 简化与 `FileRow` 类型化（零行为变化）；软删除/挂课副本行为钉死 | DONE，相关 11 tests |
| V155-C | 解析 worker 单作业超时（默认 120s、可注入、按既有 `parse_failed` 语义）；主窗口 `setWindowOpenHandler` 拒绝 + `will-navigate` 白名单守卫 | DONE，相关 9 tests |
| V155-D | 课件版本发布改为含软删除文件的锚定 MAX+1（软删不重号、手工高版本号计入）；`isConstraintError` 错误码优先、消息兜底 | DONE，相关 32 tests |

## 自动质量门（V155-E）

- 全量测试：**59 files / 238 tests passed，1 skipped**（V1.5.4 基线为 57 files / 215 tests，净增 2 files / 23 tests）；
- `npm run typecheck`：通过（0 错误）；
- `npm run lint`：通过；
- `npm run build`（production）：通过；
- `git diff --check`：通过；
- 未运行 `package:portable`，未生成 portable / installer / 对外交付包。

## 隔离启动 Smoke

- 使用独立 `TEACHER_WORKBENCH_L01_SMOKE_APP_DATA` app-data 与 `--user-data-dir` 启动 production Electron；
- 主窗口进程正常存活，隔离 `TeacherWorkspace` 工作区与 `workspace.db` / `search.db`（含 WAL）成功创建；
- 未接触正式工作区、真实教学资料或 API Key；冒烟后进程与临时目录已清理。

## 已接受的既有设计（记录在案）

- **树操作 O(全表) 载入**（`assertNoCycle`/`collectSubtreeIds`/SearchService 范围解析）：单教师规模内无碍，维持接受；revisit 条件为节点数 > ~5k，届时按 `parent_id` 局部查询优化。
- **不做带版本列的 schema v16**：名称法锚定 MAX+1 已满足单教师规模，记录在案。

## 行为不变性说明

本增量不改任何用户可见行为与文案：AI 提示词逐字不变；素材库 overview 返回内容不变（含"已移除"分视图）；版本发布命名格式不变（仅编号计算更稳健）；解析超时仅在原本会无限卡死的场景生效。

## 体验确认记录（2026-09-01）

产品负责人已在真实窗口完成最终体验确认（与 V1.5.6 合并走查，2026-09-01 裁决），以下 4 点全部通过：

1. 打开"教学内容"进入任一课次的 AI 备课：旧修改节点（V1.5.4 及以前生成）打开后修改方式/要求仍正确还原；
2. 发起一次"修改这份"或"整课重做"：方案→确认→生成→"保存为新版本"，编号连续；
3. 素材库打开正常（文件/文件夹/已移除视图齐全）；
4. 全局搜索可用（索引状态正常）。

最终确认提交 `v1.5.5(V155-E): record final acceptance` 与通过标签 `checkpoint-V1.5.5-pass` 一并创建。
