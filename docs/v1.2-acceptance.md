# 教师工作台 V1.2 全量回归与版本验收

日期：2026-08-23

最终状态：自动质量门、代表性本地 Windows 流程、安全回归和产品负责人最终体验确认均为 PASS。

标签状态：`checkpoint-V1.2-pass` 创建在最终确认提交。

## 1. 候选范围与本地里程碑

- 固定起点：`checkpoint-V1.1-pass`，位于 `a56f744`。
- V1.2 冻结方案：`d4bc32a plan(V1.2): freeze course and student architecture milestones`。
- V12-01：`5ec6c89 v1.2(V12-01): add course progress and attendance core`。
- V12-02：`9ff4be7 v1.2(V12-02): rebuild course dashboard and teaching flow`。
- V12-03：`b64922f v1.2(V12-03): add student history and manual records`。
- V12-04：`728c5dd v1.2(V12-04): connect viewed lesson files and prep`。
- V12-05 候选增加全量验收测试、Windows `EPERM` 瞬时文件锁的有限异步发布重试、本文档和状态记录；不扩大冻结的 V1.2 产品范围。

V1.2 继续复用 NodeService、CoreDataService、ManagedFileService、DraftService、LessonPrepContext、Search、Parser、AI、Backup；没有重写 V1.1 备课内核。

## 2. 自动质量门

在当前候选工作树运行：

```text
npm test             PASS · 42 files / 133 tests
npm run typecheck    PASS
npm run lint         PASS
npm run build        PASS
git diff --check     PASS
```

production build 结果：

- Main：44 modules，`out/main/index.js` 268.71 kB；
- Preload：10 modules，`out/preload/index.js` 29.86 kB；
- Renderer：53 modules，CSS 41.98 kB，JS 738.16 kB。

没有运行 `npm run package:portable`、electron-builder、portable 或 installer 流程；没有生成 V1.2 对外交付包。仓库中已有的 `release` / `release-l12` 是 V1/V1.1 历史忽略产物，本次未更新。

## 3. V1.2 一体化验收测试

`tests/v1.2-acceptance.test.ts` 使用隔离 Windows 临时工作区、真实 schema v12、Main services、SQLite、managed 对象目录、Search、DraftService、BackupRestoreService 和本地 fake AI gateway，完成同一条流程：

```text
新建三位学生和班课
→ 创建 2026 秋季 / 2027 春季及四个课次
→ 第 8 课设为 Current
→ 第 8 课 23:55、第 9 课次日 00:05，按本地日界只列第 8 课
→ 打开第 9 课首次点名后增加学生，旧名单保存被拒绝
→ 重新打开后保存并修改三人点名，Current 不变
→ 为第 9 课创建独立 managed 资料副本
→ 生成、编辑并保存第 9 课讲义，Current 仍为第 8 课
→ 第 9 课先确认已上，保持第 8 课 Current
→ 第 8 课补点名并确认，建议跳过已确认的第 9 课到第 10 课
→ 确认阶段末第 10 课并清空 Current，不跨到已存在的下一阶段
→ 关闭并重开工作区，暂停边界保持
→ 手工开始 2027 春季
→ 学生退出后，第 9 课历史点名快照仍保留
→ 结束并重开课程，春季 Current 指针保持
→ 备份并恢复到新目录
→ 课程树、进度、点名、lesson_files、历史 student_files、saved 草稿、manual 记录和搜索均恢复
```

结果：PASS。恢复库 `integrity_check=ok`、foreign_key_check 0；备份恢复重新索引 3 个 managed 文件，失败 0。外部原文件内容在复制、生成、编辑、保存、备份和恢复前后保持一致。

全量并行回归首次发现 Windows 在发布恢复 staging 时偶发一次 `EPERM`。BackupRestoreService 现仅对 `EPERM` / `EBUSY` / `EACCES` 做最多 5 次 rename 尝试，重试前异步递增等待 50–200 ms；其他错误立即返回，最终失败仍保留原有错误边界。`tests/backup-publish-retry.test.ts` 模拟首次 `EPERM`，确认第二次原子 rename 成功且不会发布半成品。

## 4. 代表性本地 Windows production 流程

使用当前 `npm run build` 产物启动 Electron，设置新的隔离 `TEACHER_WORKBENCH_L01_SMOKE_APP_DATA` 和 `user-data-dir`，通过本机 DevTools 驱动真实 Renderer → 类型化 Preload → Main/Service → SQLite；未接触正式工作区、真实教学资料或真实 API Key。

已验证：

- 唯一窗口标题为“教师工作台”，viewport 为 1264 × 781；
- 创建 3 位假学生、1 门班课、2 个阶段和 4 个课次；第 8 课为 Current；
- 第 8 课本地 23:55 出现在今日待点名，第 9 课次日 00:05 不进入当天列表；
- 打开两人点名名单后新增第 3 位学生，旧名单保存返回“课程学生名单已经变化，请重新打开点名”；重新读取后可保存并修改三人点名；
- 第 9 课先点名、先确认已上，Current 仍为第 8 课；
- 从第 9 课 Viewed Lesson 的资料页进入“本次备课课次”，Prep 为第 9 课且 Current 仍为第 8 课；
- 第 8 课补点名并确认后 Current 设为第 10 课；确认阶段末第 10 课后 Current 为空、active period 仍为 2026 秋季；
- 关闭并重启同一隔离工作区，阶段边界和 manual 学习记录仍存在；
- 手工开始 2027 春季后，一位学生退出，历史第 9 课点名仍为 3 人；
- 结束并重开课程后，2027 春季 / 春季第一课 Current 指针、课程树和 manual 记录均保留；
- 学生详情把退出学生的课程归入“已退出 / 已结束”，并继续显示 manual 记录；
- StudentsPage 截图捕获成功（PNG 约 60,435 bytes），页面正常渲染且无可见错误提示。

最终隔离库：schema v12、`integrity_check=ok`、foreign_key_check 0；1 门课程、2 个阶段、4 个课次、3 位学生、1 条 ended 学生关系、3 条 lesson session、6 条 attendance row、1 条 manual 记录。应用正常关闭后两个明确临时目录均已删除。

### 2026-08-24 · 产品确认前基础复核

- 再次运行 `npm test`、typecheck、lint、production build 与 `git diff --check`：42 files / 133 tests 全部通过；Main / Preload / Renderer 构建结果与候选记录一致。
- 单独以 verbose 重跑 V1.2 core、课程 UI、学生 UI、备课资料与一体化验收：6 files / 19 tests 全部通过。
- 当前 production build 在新的 Windows 临时隔离工作区中通过真实 Renderer → Preload → Main/Service → SQLite 完成：新建学生、事务创建班课并关联学生、创建阶段与第一课、第一课初始化为 Current、保存点名且 Current 不变、确认本课已上后停在阶段边界。
- 关闭并重启同一隔离工作区后，课程、学生在读关系、已上、点名和阶段边界状态均保留；学生页仍显示对应在读课程。
- 最终隔离库为 schema v12、`integrity_check=ok`、foreign_key_check 0；包含 1 门课程、1 个阶段、1 个课次、1 位学生、1 条点名和 1 条已上记录。验证应用正常关闭，明确的临时目录与测试数据随后已删除。
- 路径安全边界额外阻止了把测试工作区放入应用安装目录子树；改用 Windows 临时目录后流程正常。未接触正式数据、真实资料或 API Key。

本次复核不等同于产品负责人最终体验确认；`checkpoint-V1.2-pass` 仍不创建。

## 5. 安全与兼容回归

| 边界 | 证据 | 结果 |
|---|---|---|
| Renderer 隔离 | `tests/renderer-boundary.test.ts` 扫描 Renderer import、动态 import、require 与 Node globals | PASS |
| 严格 IPC | core / attendance / file / AI / draft IPC 测试验证白名单、运行时 payload/response schema 和稳定错误 | PASS |
| 进度事务 | 过期 `expectedCurrentLessonId`、跨课程引用、重复确认、节点移动/删除、ended course 均有 Service 测试 | PASS |
| 点名事务 | 首次名单变化拒绝、历史快照严格匹配、完整名单替换、无排课也可点名 | PASS |
| 路径与原资料 | managed 对象路径、根外拒绝、临时写入/原子改名、独立副本和原件不覆盖测试 | PASS |
| Key 安全 | secure/session storage、日志脱敏、IPC/错误/数据库/备份负向测试 | PASS |
| Search / Parser | 中文/数学搜索、索引重建、DOCX/PPTX/PDF/XLSX 解析和顺序 Worker 全量回归 | PASS |
| Backup / Restore | schema v12 数据、managed 文件、lesson/student 历史关联和派生搜索重建 | PASS |
| Git 内容 | `.gitignore` 排除运行库、索引、日志、备份、Key、真实资料、依赖和 build/package 输出 | PASS |

全局素材库和学生页均不再暴露学生文件目标；`student_files` 与 `ManagedFileService.copyToStudent()` 继续保留，历史数据、搜索和备份恢复兼容测试通过。

## 6. 已知限制 / Later

- 不提供排课日历、重复规则、提醒、迟到分钟、出勤率/报表或课程百分比。
- 不提供学生画像、成绩分析、学生文件 UI、学习记录附件或复杂 enrollment 区间历史。
- 一个 lesson 仍表示一次计划课次；同一内容再上一节时创建或复制新课次，不增加多 session 模型。
- 不提供文件共享、多个外部 root、OCR、向量搜索、流式 AI、新 AI 工作流、Workflow/Agent 或草稿版本树。
- V1.2 不创建 portable、installer 或对外交付包；如未来明确要求发布，应作为独立授权任务执行。

上述均为冻结方案明确的 Later，不阻塞 V1.2。

## 7. 最终产品体验确认 · PASS

2026-08-24，产品负责人确认当前 V1.2 没有问题，并明确授权上传 GitHub。此前完成的自动质量门、代表性 Windows 流程、安全回归和产品确认共同构成最终通过证据。

- 将本确认记录提交到本地 `main`；
- 在该最终确认提交创建 annotated tag `checkpoint-V1.2-pass`；
- 按用户本次明确授权推送 `main` 与该标签到现有 GitHub `origin`；
- 不添加或修改远程，不生成 portable、installer 或对外交付包。

结论：V1.2 最终体验通过。
