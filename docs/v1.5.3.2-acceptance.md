# 教师工作台 V1.5.3.2 验收补充报告（素材库与软件级弹窗）

**日期：** 2026-08-31

**基线：** checkpoint-V1.5.2-pass；V153-A～V1532-C 已完成

**结论：** 产品负责人已确认素材库逻辑目录、课次资料流转与软件级弹窗体验通过。本补充范围随 V1.5.3 一起纳入 checkpoint-V1.5.3-pass，不创建独立的 V1.5.3.2 checkpoint。

## 1. 验收范围

- 素材库使用“全部素材 / 最近使用 / 待整理 / 我的文件夹”结构；老师自建文件夹支持嵌套层级。
- 外部资料、素材库原件、课次独立副本和学生附件保持来源与生命周期隔离。
- 课次资料“从本课移除”只处理本课独立副本，不影响素材库原件。
- 新建、重命名、删除素材库文件夹使用软件内菜单、文本输入框和确认框。
- AI 修改、快速建课、发布、切换修改记录等确认动作不再调用 Windows 系统级 window.confirm / window.prompt / window.alert。

## 2. 自动质量门

- npm test：57 files / 213 tests passed，1 skipped；
- npm run typecheck：通过；
- npm run lint：通过；
- npm run build：通过；
- git diff --check：通过；
- Renderer 搜索 window.confirm / window.prompt / window.alert：无结果；
- 未运行 portable/installer，未提交真实教学资料、数据库、密钥或日志。

## 3. 代表性流程

产品负责人已完成两棵资料树、目录维护、外部资料保存到素材库、素材复制到课次、从本课移除及重启后状态保持的体验确认；软件内确认框的取消、关闭、破坏性操作和连续请求排队行为符合预期。

## 4. 版本动作

- 里程碑提交：3d28622 v1.5.3.2(V1532-D): replace native dialogs with app dialogs；
- 通过标签：checkpoint-V1.5.3-pass；
- GitHub 远程：origin；
- 未创建独立 checkpoint-V1.5.3.2-pass。
