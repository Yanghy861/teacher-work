# 教师工作台 V1.5「教学内容工作台」产品与实施方案

**日期：** 2026-08-26

**稳定基线：** `checkpoint-V1.4-pass`

**状态：** V1.5 主基线已冻结；本轮实施链 V15-01～V15-03 整体记为 V1.5.1；本文末尾追加“V1.5.2 AI 修改工作区方案”，用于区分后续增量，不创建 V1.6

## 1. 问题

现有产品把“浏览课件”“执行备课”和“管理课程”挤在两个不合适的位置：

- 双击课次直接进入全局“备课”，把打开已有内容误当成开始任务；
- 把课件长期放在“课程”详情里，会让课程管理、课程课次目录、课件目录和正文形成四列，正文宽度明显不足；
- 课件、备课和修改过程本来共享同一个 lesson 上下文，却需要在不同全局页面间来回切换。

V1.5 要同时保住两件事：课程管理边界清楚，教学正文仍有足够阅读空间。

## 2. 冻结结论

左侧主导航新增“教学内容”，替代原全局“备课”入口。“课程”只负责课程和教学事实；“教学内容”统一承载课件、AI 备课和修改记录。

```text
课程：课程、阶段、课次、学生、排课、点名、Current Lesson、确认已上
教学内容：本课课件、AI 备课工作区、修改记录
素材库：跨课程复用的公共素材
```

“课件”不成为第二个全局导航，也不继续作为课程详情中的常驻正文区。课件仍归属具体 lesson，教学内容工作台只是统一打开和处理它们的地方。

## 3. 左侧主导航

V1.5 的主导航为：

```text
课程
搜索
题库
外部资料
素材库
学生
教学内容
设置
```

- “我的课程”统一改名为“课程”；
- 原“备课”主导航移除，能力迁入“教学内容”的“AI 备课”和“修改记录”分区；
- 不增加独立“课件”主导航；
- 现有草稿数据、备课内核和 managed 文件不迁移、不复制。

## 4. 教学内容工作台

### 4.1 顶部课次上下文

工作台顶部始终显示：

```text
课程 / 阶段 / 课次
```

并提供：

- “切换课程 / 课次”；
- “上一课”“下一课”；
- 可选“返回课程”或“返回学生”；
- “沉浸阅读”（仅课件分区）。

课程与课次目录默认不常驻。点击“切换课程 / 课次”时，从左侧临时滑出选择抽屉；选择后自动收回。上一课和下一课只改变 Renderer 中的 Viewed Lesson，不改变 Current Lesson、授课事实或课程进度。

### 4.2 三个固定分区

教学内容工作台内部固定为：

```text
课件｜AI 备课｜修改记录
```

切换分区不丢失当前 course、period 和 lesson。

#### 课件

- 左侧只保留当前 lesson 的课件目录，建议宽度 220–260px；
- 右侧正文占用其余全部工作区宽度；
- 不同时常驻课程目录和课件目录；
- 复用 LessonMaterialReader，支持 Markdown、图片、普通文件、正文引用素材、刷新、系统打开和定位文件；
- “沉浸阅读”临时收起本课课件目录，让正文占满工作区；
- 空课次明确显示“本课还没有课件”，并提供“开始备课”；
- 已结束课程和学生历史课程仍可只读浏览已有课件。

#### AI 备课

- 复用 V1.1 的 LessonPrepContext、资料选择、Skill、本次要求、AI 生成、预览、编辑和保存能力；
- 当前 lesson 自动成为 Prep Lesson；
- 外部资料和素材库仍通过既有独立副本流程加入本课；
- 支持“新建本课内容”和“基于已有课件重新备课”两种模式；
- 老师的修改要求、参考内容、AI 方案和生成结果都属于当前修改工作区；
- 任何时间都可以“保存当前进度”，保存的是工作副本，不改变正式课件；
- 老师确认后才可“保存为新版本”，旧正式课件继续保留；
- AI 不直接覆盖正式课件。

#### 修改记录

- 展示当前课次的修改会话、AI 方案、工作副本保存节点和正式版本节点；
- 记录老师提出的修改要求、使用的参考内容、AI 执行结果和老师的确认动作；
- 任意修改节点都可以打开、恢复或继续修改；
- 未发布节点明确标记为“修改中”，正式版本明确标记为“已确认”；
- 不把修改记录提升为左侧主导航，也不建立独立全局草稿库；
- 既有 draft 数据继续兼容使用，但在界面中解释为当前工作副本或修改节点。

### 4.3 两种备课模式

#### 新建本课内容

适用于当前 lesson 没有可用正式课件：

`text
开始备课
→ 选择参考资料
→ 填写老师要求
→ AI 生成工作副本
→ 老师编辑并随时保存
→ 保存为本课新版本
`

#### 基于已有课件重新备课

适用于当前 lesson 已有正式课件：

`text
已有课件
→ 作为参考创建修改工作区
→ 老师说明这次想怎么改
→ AI 先提出修改方案
→ 老师确认方案
→ AI 生成新工作副本
→ 新旧内容对比、编辑、随时保存
→ 保存为新的正式版本
`

“编辑本课课件”是手动编辑正式内容的入口；“基于已有课件重新备课”是 AI 协作入口。两者不能合并成一个含义不清的“修改课程”按钮。

### 4.4 保存与版本语义

教学内容必须区分：

`text
正式课件：老师确认后可用于上课的版本
当前工作副本：老师和 AI 正在协作修改的内容
修改记录：本次 Agent 会话和所有中间节点
正式新版本：老师确认后发布到本课课件的版本
`

- “保存当前进度”：任何时间可用；只保存当前工作副本和修改上下文，不改变正式课件；
- “自动保存”：在编辑、AI 方案生成和长任务阶段保留可恢复节点，不能替代显式发布；
- “保存为新版本”：老师确认后执行；新版本进入本课课件，旧版本保留；
- “放弃本次修改”：只放弃当前工作副本，不删除旧正式课件；
- 重新进入课次时，优先恢复最近未发布的工作副本，并明确提示其未成为正式课件。

### 4.5 AI 协作边界

- AI 只能读取老师明确选择的正式课件、课次资料和可选课堂记录；
- AI 先给出可读的修改方案，再按老师确认的方案生成内容；
- AI 生成结果始终写入工作副本或新草稿节点，不覆盖原始资料和正式课件；
- 老师可以拒绝方案、修改要求、重新生成、编辑结果或恢复旧节点；
- 生成失败、取消或 Key 不可用时，已有正式课件和已保存工作副本仍可继续使用；
- 任何 AI 结果都必须能追溯到参考内容和本次老师要求。

### 4.6 正文宽度

宽窗口默认布局固定为：

```text
全局主导航｜本课课件目录｜正文
```

不得变成：

```text
全局主导航｜课程课次目录｜本课课件目录｜正文
```

在约 1200px 的代表性窗口中，课程 / 课次选择抽屉关闭后，正文应获得扣除主导航和 220–260px 本课课件目录后的全部剩余宽度，视觉阅读面积不得小于当前已有课次资料阅读页。窄窗口下，本课课件目录可以折叠或落到正文上方，但不得造成页面级横向溢出。

## 5. 入口与返回

### 5.1 从课程进入

- 课程页继续显示课程、阶段、课次、学生和进度；
- 单击课次只选择 Viewed Lesson；
- 双击课次和可见“查看教学内容”进入“教学内容 / 课件”；
- “开始备课 / 继续备课”进入同一 lesson 的“教学内容 / 备课”；
- 课程卡上原有明确备课动作按同样规则进入“教学内容 / 备课”；
- 返回时恢复原课程和课次选择，不改变 Current Lesson。

### 5.2 从学生进入

- 学生页继续显示在读课程和历史课程；
- 进入课程后使用同一课程页面，不复制学生专属课程树；
- 从课程课次进入教学内容时保留来源学生；
- “返回学生”恢复原学生选择；历史课程仍可只读查看课件。

### 5.3 直接打开“教学内容”

- 优先恢复本次应用会话内最近一次仍有效的 course / lesson 和分区；
- 没有有效最近位置时，显示轻量空状态和“选择课程 / 课次”，打开同一个临时抽屉；
- 不把最近浏览位置写入 SQLite，不为用户猜测或修改 Current Lesson；
- 直接点击“修改记录”时允许没有预选 lesson；打开具体节点后再建立上下文。

## 6. 三种课次语义继续分离

- Current Lesson：课程进度中默认下一次处理的课次，持久化；
- Viewed Lesson：当前教学内容工作台正在浏览的课次，仅 Renderer 状态；
- Prep Lesson：当前备课使用的课次，由 LessonPrepContext 决定。

浏览、切换课件、打开修改节点或进入备课都不得隐式修改 Current Lesson。

## 7. 数据与技术边界

- 不新增 schema，不做 migration；
- 不改变 course、period、lesson、course_students、course_progress、lesson_sessions、notes 或 files 的事实语义；
- 导航目标使用类型化 Renderer 状态，至少包含 courseId、lessonId、section 和可选 originStudentId；
- 优先复用 CourseDashboard、CourseDetail、StudentsPage、LessonFilesSection、LessonMaterialReader、DraftPanel、LessonPrepContext 和既有 Main 服务；
- 默认不新增 Service 或 IPC；若现有安全白名单能力确实不足，必须停止并重新确认范围；
- Renderer 仍不能接触 SQLite、Node、任意路径或 Key；
- 外部资料只读、managed 原子写入、AI 不覆盖原资料、题库快照只读和备份边界不变。

## 8. 不做

当前 V15-01～V15-03 不加入第二棵课程树、独立课件库、课程级共享文件、拖拽文件整理、课件版本管理、多人协作、云同步、成绩分析、错题本、推荐、组卷、多题批量入课、新 AI Workflow 或数据模型重构。课件版本、工作副本和 Agent 修改记录属于本文第 11 节的 V1.5.2 方案，不提前计入当前任务验收。

## 9. 冻结的实施切片

1. `V15-01`：教学内容导航目标、课程 / 学生入口、直接打开与双向返回；
2. `V15-02`：教学内容工作台、临时课次抽屉、宽正文、课件 / AI 备课 / 修改记录三分区；
3. `V15-03`：全量回归、production build、安全审计和代表性本地 Windows 流程。

## 10. 验收

1. 左侧显示“课程”和“教学内容”，不再显示“我的课程”或独立“备课”；
2. 课程页只承担课程管理；双击课次打开“教学内容 / 课件”，明确备课按钮打开“教学内容 / 备课”；
3. 教学内容中“课件 / AI 备课 / 修改记录”共享并保持同一个 course / lesson 上下文；
4. 课程 / 课次目录默认不常驻；临时抽屉、上一课和下一课可切换 Viewed Lesson，Current Lesson 不变；
5. 约 1200px 代表性窗口中只常驻主导航和 220–260px 本课课件目录，正文阅读面积不小于现有课次资料阅读页；
6. 沉浸阅读能收起本课课件目录；退出后恢复目录和原文位置；
7. 从课程和学生进入、返回，以及直接打开教学内容三条路径都能恢复合理上下文；
8. 当前 V15-01～V15-03 的课件、备课和既有草稿兼容共享同一个 course / lesson 上下文；
9. 课程管理、Current Lesson、点名、确认已上、快速建课、题库加入课次、素材库、搜索和 V1.1 备课内核无回归；
10. 全量测试、typecheck、lint、production build、`git diff --check` 和代表性 Windows 流程通过；
11. 不运行 portable / installer，不自动 push；产品负责人最终体验确认前不创建 `checkpoint-V1.5-pass`。

## 11. V1.5.2 产品方案：AI 修改工作区

**编号裁决（2026-08-29，产品负责人）：** V1.5 是版本基线（通过标签仍为 `checkpoint-V1.5-pass`）；刚完成的「教学内容工作台」实施链 V15-01～V15-03 作为整体记为 **V1.5.1**；本轮之后的下一轮增量「AI 修改工作区」记为 **V1.5.2**；未来同类增量按 V1.5.3、V1.5.4 顺序递增。V1.5.2 不是 V1.6。

V1.5.2 在当前 V15-01～V15-03 教学内容工作台之上继续演进，不改变课程管理、Current Lesson、Viewed Lesson、Prep Lesson 或既有安全边界。

### 11.1 终极目标

备课从“一次性生成草稿”升级为：

> 老师与 AI 围绕一节课持续协作，保留每次修改的意图、过程和结果；任何时间可保存，只有老师确认后才发布为新的正式课件。

### 11.2 建议的后续实施顺序

1. **V1.5.2-A：术语和界面收口**  
   将“草稿箱”改名为“AI 备课”与“修改记录”，移除全局草稿心智；保留现有 notes 数据兼容，不做数据迁移。
2. **V1.5.2-B：当前工作副本**  
   为当前 lesson 建立可恢复的工作副本状态，支持显式保存、离开后继续和未发布提示。
3. **V1.5.2-C：已有课件改进流程**  
   增加参考范围、老师修改要求、AI 修改方案确认、新旧内容对比和重新生成。
4. **V1.5.2-D：修改记录与版本发布**  
   记录 Agent 会话节点、恢复节点和正式版本发布；保存为新版本后回到课件区查看。

V1.5.2 的 A～D 仍是一条增量链；是否进入实现，以当前 V15-03 最终体验确认后的产品负责人确认作为前置条件。若未来继续增加同类增量，按 V1.5.3、V1.5.4 顺序递增，不回写 V1.5 基线。
