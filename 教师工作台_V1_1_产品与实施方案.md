# 教师工作台 V1.1 产品与实施方案

**状态：** 已确认，等待实施

**更新日期：** 2026-08-22

**实施基线：** `checkpoint-L12-pass`

**适用版本：** V1.1

## 1. 文档优先级

本文件是 V1.1 的产品主规格。V1 已完成并保持冻结；V1.1 只在现有成品上做增量更新。

当文字规格与参考图冲突时，以文字规格为准。参考图只用于说明布局、信息层级和交互关系，不约束具体导航名称、颜色、用户名、按钮数量、假数据或 Word/PDF 高保真渲染效果。

## 2. V1.1 目标

让老师自然完成一条固定的备课流程：

```text
我的课程
→ 选择课次
→ 开始备课
→ 从外部资料或素材库添加本次资料
→ 选择 Skill（可选）
→ 填写本次要求（可选）
→ 生成讲义 / 例题 / 作业
→ 在同一区域预览或编辑
→ 保存修改
→ 保存到当前课次
```

老师不需要理解工作流、节点、变量或 Agent。

## 3. 外部资料浏览

### 3.1 定位

外部资料用于快速浏览老师电脑中已有的 PPTX、DOCX、PDF、MD、TXT、XLSX 等参考资料，不是一个新的完整文件管理器。

V1.1 第一版只支持一个外部资料根目录。老师可以在设置中选择或更改该目录。

### 3.2 布局

```text
全局导航 │ 可折叠资料树 │ 内容区
```

资料功能只包含资料树和内容区，不增加第三个永久文件列表栏。

资料树直接显示真实文件夹和文件，文件夹按点击动作 lazy 展开。折叠资料树后内容区自动扩展，并保留重新打开资料树的按钮。

### 3.3 内容区

选中文件夹时显示简单目录信息。选中文件时至少显示：

- 文件名；
- 文件类型；
- 大小；
- 修改时间；
- 打开文件；
- 所在文件夹；
- 有课次上下文时显示“用于本次备课”；
- 无课次上下文时显示“复制到素材库”。

V1.1 不做 Office/PDF 高保真内嵌渲染。MD/TXT 可在成本较低时显示受限长度的文本预览；其他格式允许显示文件信息并使用系统应用打开。

### 3.4 安全规则

- Renderer 不接触任意绝对路径，只使用类型化 IPC、外部根 ID 和相对路径；
- Main 每次操作都验证目标真实路径仍位于所选根目录内；
- 外部资料默认只读，工作台不删除、移动或重命名真实文件；
- “用于本次备课”或“复制到素材库”必须复制成 managed 独立副本；
- managed 正式文件继续使用临时文件、校验和原子重命名；
- 不做 watcher、全盘后台扫描、多 root、外部资料全文索引或长期同步。

## 4. 当前课次与本次备课资料

备课的主入口从全局手工选择学生/课次，调整为：

```text
我的课程 → 某个课次 → 开始备课
```

页面自动知道当前课程和当前课次。一对一课程可以自动带入关联学生；班课和没有学生的课次不强制选择学生。AI 草稿必须绑定课次，不再强制绑定学生。

备课页面左侧显示本次课已经加入的 managed 独立资料，并提供：

```text
[从外部资料添加]
[从素材库添加]
```

两种来源加入课次后都形成新的 managed 独立副本，并自动作为当前生成动作的已选来源。实际保存的是课次文件关系，不新增复杂的“备课会话”或 Workflow 数据库。

## 5. Skill 与本次要求

Skill 只是一段老师保存并重复使用的 Prompt 模板。一个 Skill 只包含：

- 名称；
- Prompt 内容；
- 创建时间；
- 修改时间；
- 软删除状态。

支持新建、编辑、删除和选择。Skill 可为空，不包含参数系统、节点、连线或执行逻辑。

“本次要求”是只用于当前生成请求的普通文本，可为空，不修改 Skill。普通备课界面不展示字符/token 等技术参数，继续由安全默认值控制上下文预算。

## 6. 固定 AI 动作

V1.1 继续复用现有 OpenAI-compatible AI Gateway、ContextBuilder 和三个 DraftKind：

```text
lecture  → 生成讲义
example  → 生成例题
homework → 生成作业
```

Prompt 固定组合为：

```text
当前课次信息
+ 老师明确选择的资料
+ 选中的 Skill（可选）
+ 本次要求（可选）
+ 当前固定生成任务
```

资料内容与操作指令必须分区组织。只发送老师明确选择且经过预算限制的文本，继续保留来源信息。Skill 修改后不得改变历史草稿，草稿元数据保存生成时使用的必要快照。

## 7. 草稿箱、预览、编辑与保存

### 7.1 简单生命周期

```text
AI 生成完成
→ 自动保存为草稿，避免退出后丢失
→ 预览 / 编辑 / 保存修改
→ 保存到当前课次
→ 状态从 draft 变为 saved
```

正文始终只有一份，通过状态区分草稿和正式课次成果，不复制两个相同内容副本。

### 7.2 草稿箱

全局“备课”入口显示尚未保存到课次的草稿，至少展示所属课程、课次、类型和修改时间。课次备课页面只显示当前课次的生成结果。

允许删除不再需要的草稿。删除采用普通软删除，不建立回收审批、版本树或审计日志。

### 7.3 重新生成

重新生成创建一份新草稿并切换到新结果，旧草稿继续保留在草稿箱。若当前编辑区存在未保存修改，先给出简单确认提示。重新生成不得覆盖已保存到课次的成果，也不得覆盖老师原资料。

### 7.4 同一区域预览与编辑

点击结果后，右侧同一个内容容器默认显示预览。点击“编辑”后，该容器原地切换为编辑状态，不跳转页面，不在页面底部创建第二个编辑器。

预览状态操作：

```text
[编辑] [重新生成] [保存到当前课次]
```

编辑状态操作：

```text
[取消编辑] [保存修改] [保存到当前课次]
```

保存修改后回到预览，并立即显示新内容。

## 8. 明确不做

- Coze 式 Workflow；
- 节点、分支、循环、变量连线、脚本节点或 HTTP 节点；
- 多 Agent；
- Prompt Graph；
- 草稿版本树、审批流、审计日志或复杂状态机；
- 外部目录 watcher、全盘扫描、长期同步和多 root；
- 外部资料全文搜索；
- Office/PDF 高保真内嵌预览；
- 大规模性能矩阵和企业级故障注入。

## 9. 轻量实施链

```text
checkpoint-L12-pass
        ↓
V11-01 外部资料浏览
        ↓
V11-02 课次备课入口与本次资料
        ↓
V11-03 Skill、本次要求与 Prompt 组合
        ↓
V11-04 草稿箱、预览、原地编辑与保存
        ↓
V11-05 V1.1 回归与 Windows 交付
        ↓
checkpoint-V1.1-pass
```

普通里程碑只运行相关测试、typecheck 和 lint；只有 V11-05 运行完整测试、production build、portable packaging 和一条代表性 Windows 流程。只对路径逃逸、原资料覆盖/串改、Renderer 越权和 API Key 泄漏等高风险边界做必要负向验证。

## 10. 用户参与

实施过程中只需要产品负责人参与两个实际体验点：

1. V11-02 完成后，体验“课次 → 外部资料 → 用于本次备课”；
2. V11-05 完成后，用不敏感的真实 PPTX、DOCX、PDF 走完一次完整备课流程。

真实 API Key 只由老师在本地应用设置中输入，不发送到聊天、不写入任务文件或 Git。
