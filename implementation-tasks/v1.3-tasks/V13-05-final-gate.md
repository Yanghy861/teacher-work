# V13-05 · V1.3 全量回归与版本验收

**前置：** V13-01–V13-04 全部 `DONE`。

**结果：** 证明 V1.3 快速建课完整可用，V1.2 课程 / 学生 / 点名 / 进度与 V1.1 备课内核未退化。

## 自动质量门

- `npm test`
- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `git diff --check`

不得运行 `package:portable`，不得生成 portable、installer 或对外交付包。

## 代表性本地 Windows 流程

```text
快速建课 → 粘贴已有 / 新建 / 同名学生 → 处理重名
→ 2026 秋季 → 空课次与教学计划分别验证 1/100/101 边界
→ 每周 / 每两周规律排课 → 排除、顺延、改单节、清空单节
→ 寒暑假自由多选日期 → 部分课次保留未排
→ 暂不排课但保留 90 分钟时长
→ 检查页三种摘要 → 原子创建 → 回到新课程
→ 第 1 课为 Current、全部未确认已上、点名未产生
→ 现有逐项建课、学生关系、设置时间、点名、软推进与备课仍可用
→ 重启后课程、关系、课次、日期、时长和 Current 均保留
```

## 安全与兼容

- Renderer 仍只使用运行时校验的类型化白名单 IPC，不接触 SQLite、Node、任意路径或 Key。
- 原子失败不得留下半套课程或新学生；100 节边界和输入重校验在 Main 生效。
- 外部原件、managed 原资料、API Key、运行数据库、索引和备份不进入 Git，也不被快速建课触碰。
- V1.1 备课、Parser、Search、AI、Backup 与 V1.2 点名 / 进度全量回归通过。

完成证据写入 `docs/v1.3-acceptance.md`。自动门、代表性 Windows 流程和产品负责人最终体验均通过后，才在最终提交创建 `checkpoint-V1.3-pass`；不自动 push。
