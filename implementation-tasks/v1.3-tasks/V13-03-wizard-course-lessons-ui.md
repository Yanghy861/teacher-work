# V13-03 · 快速建课前两步 UI

**前置：** V13-02 `DONE`。

**结果：** 完成可访问、可返回修改的“课程与学生”“阶段与课次”两步向导 UI；未完成排课与确认前不替换课程页正式入口。

## 最小范围

- 可关闭的四步向导容器、步骤条、返回 / 下一步和放弃确认。
- 第一步：课程名称、班课 / 一对一、粘贴名单、解析状态、已有 / 新建标记。
- 同名学生确认弹层，只展示现有课程关系和最近人工记录；支持选已有或明确新建同名学生。
- 有重名待确认时禁用“下一步”；一对一人数和空课程提示符合冻结规则。
- 第二步：阶段推荐可编辑；空课次 / 教学计划两种模式；即时编号预览与 100 节上限。
- 返回上一步不丢输入；关闭有内容的向导时要求确认放弃。
- 组件测试覆盖键盘可达、错误状态和主要交互，不提前接入第三 / 四步假实现。

## 验证

- 运行向导前两步相关组件与 view-model 测试。
- `npm run typecheck`
- `npm run lint`
- `npm run build`
- 隔离 Renderer / Electron UI smoke：名单解析、重名处理、空课次与计划粘贴。
- `git diff --check`
