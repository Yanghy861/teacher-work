# V152-D · 修改记录与版本发布

**前置：** V152-C `DONE`。

## 范围

- 修改记录分区展示当前课次的修改节点：老师要求、参考内容、AI 方案、生成/保存节点，按时间排列；
- 任意节点可打开、恢复或继续修改；未发布节点标记"修改中"，老师确认的成果节点标记"已确认"；
- "保存为新版本"：老师确认后把工作副本发布为本课正式课件（新 managed 副本入课，旧版本保留），发布后回到课件区查看；
- 不把修改记录提升为左侧主导航，不建立独立全局记录页。

## 边界（2026-08-29 产品负责人批准）

- "由文本创建正式 managed 课件"超出现有白名单能力，产品负责人已批准**选项 1**：新增窄白名单通道 `drafts:publish-to-lesson`。Main 侧完全复用既有 managed 写入规则（同目录临时文件 + 原子重命名、对象路径锁死工作区、UUID 目录、SQLite 事务登记、lesson_files 关联、入索引队列），不触碰外部原件；仅此一条通道，不做通用文件写入。
- 版本语义：发布的课件文件按"原标题 · 第 N 版"命名入课（N 为该课次已发布版数次 +1），课件目录排序新版本在前；旧版本始终保留，修改记录发布节点标记"已确认"。
- 体验层裁决：版本折叠/归档/保留策略（如"只留最近 3 版"）记为 V1.5.3 候选，不在本轮实现；存储评估——文本课件单版本 2–10 KB，数据量不构成风险。

## 验证

- 端到端测试覆盖节点追溯、恢复、发布与旧版本保留、原资料不变；
- 隔离 Electron UI smoke；相关测试、typecheck、lint、build、`git diff --check`。
