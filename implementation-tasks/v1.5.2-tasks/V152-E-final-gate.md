# V152-E · V1.5.2 全量回归与版本验收

**前置：** V152-A–V152-D 全部 `DONE`。

## 自动质量门

- `npm test`、`npm run typecheck`、`npm run lint`、`npm run build`、`git diff --check`；
- 不运行 `package:portable`，不生成对外交付包。

## 代表性隔离 Windows 流程

```text
选择已有正式课件的课次 → AI 备课 → 发起改进
→ 选择参考范围 + 填修改要求 → AI 中继生成修改方案（真实语义内容）
→ 审阅/确认方案 → 生成新工作副本 → 新旧对比 → 保存当前进度
→ 离开并重进：恢复未发布工作副本且有未发布提示
→ 保存为新版本 → 回到课件区确认新版本可用、旧版本保留
→ 修改记录中核对要求/参考/方案/生成/发布节点与"修改中/已确认"标记
→ 快速建课、点名、确认已上、题库、搜索、备份入口无回归
```

## 验收分层（D15）

1. 固定 fake provider 自动门（状态机与安全边界）；
2. 中继式 AI 开发验收（真实语义方案与内容，实施代理执行并留痕）；
3. 产品负责人以真实 API Key 完成至少一轮完整备课后做最终体验确认。

完成证据写入 `docs/v1.5.2-acceptance.md`；自动门、代表性流程与最终体验确认齐全后，才在最终提交创建 `checkpoint-V1.5.2-pass`；不自动 push。
