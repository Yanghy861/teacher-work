# V14-02 · 工作台原生题库浏览与单题动作

**前置：** V14-01 `DONE`。

## 范围

- 在现有导航中新增“题库”，不替换工作台壳；
- 未导入空状态、搜索、筛选、分页和结果列表；
- 默认不显示永久详情；宽屏右侧详情、窄屏下方详情和主动收起；
- 图片、选项、本地公式、来源、折叠答案与解析；
- 单题导入素材库和选择 course / lesson 后加入课程；
- 保留搜索位置和当前选择，不提前加入组卷等能力。

## 验证

- UI / IPC / 复制相关测试；
- typecheck、lint、build、`git diff --check`；
- 隔离 Electron UI smoke。
