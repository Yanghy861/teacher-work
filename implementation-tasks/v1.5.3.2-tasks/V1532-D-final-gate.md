# V1532-D · 素材库逻辑目录最终回归与体验确认

**状态：** 待产品负责人确认，不进入当前活动链

**前置：** V1532-A–V1532-C 全部 `DONE`。

## 自动质量门

- `npm test`；
- `npm run typecheck`；
- `npm run lint`；
- `npm run build`；
- `git diff --check`；
- 不运行 `package:portable`，不生成 portable、installer 或对外交付包。

## 代表性 Windows 流程

在隔离工作区中执行：

```text
选择外部资料目录
→ 按真实层级展开“公共资料 / 七年级 / 三角形”
→ 选择一份文件，保存到素材库 / 七年级 / 三角形
→ 在素材库树中确认逻辑路径与文件详情
→ 新建“期中复习”文件夹并移动素材
→ 从素材库复制到课程 / 阶段 / 第 3 课
→ 在课次资料中打开并修改课次独立副本
→ 从本课移除，确认素材库原件仍可打开
→ 从另一份课次资料保存为素材，放入待整理
→ 搜索素材，确认结果显示逻辑路径
→ 重启应用，确认目录、排序、文件归属和删除状态保持
```

## 回归重点

- 外部资料真实树与素材库逻辑树互不串联；
- 素材库原件、课次副本、学生附件不会互相覆盖；
- 已有关联的课程副本不会因刷新或迁移重新出现在素材库；
- “文档 / 图片 / 其他”只作为筛选；
- AI 修改、课件版本、修改记录、搜索、题库、课程/学生页面无回归；
- 删除和恢复仍遵循现有 managed 文件安全边界；
- 备份恢复后素材库目录关系与文件事实一致。

## 验收证据

- 新增 `docs/v1.5.3.2-acceptance.md`，只记录脱敏数据和隔离工作区证据；
- 记录迁移前后文件数量、目录归属、孤儿关系和 SQLite `integrity_check`；
- 记录代表性 Windows 截图或可复现实验步骤；
- 记录未运行 portable/installer；
- 产品负责人完成逻辑目录与资料流转体验确认后，才允许创建新的通过标签。

## 标签与版本规则

- 本分支是否仍归属 V1.5.3，必须由产品负责人在方案确认时裁决；
- 若维持当前命名，使用 `v1.5.3.2(V1532-XX): <摘要>` 里程碑提交；
- 不覆盖、不移动既有 `checkpoint-V1.5.3-pass`；
- 未完成最终体验确认前，不创建新的 checkpoint；
- 不自动 push。
