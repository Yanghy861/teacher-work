# 教师工作台 V1 产品与技术实施规格

> 历史基线说明：本规格对应已完成并标记为 `checkpoint-L12-pass` 的 V1。当前 V1.1 增量产品规格见 `教师工作台_V1_1_产品与实施方案.md`。

**用途：** 作为个人开发与 Codex 实现时的主规格文档  
**目标平台：** Windows 优先，桌面应用  
**开发原则：** Local-first、简单优先、文件安全优先、AI 是增强能力而不是数据底座  
**文档状态：** V1 范围冻结 + 三项工程风险加固版（R3）
**更新日期：** 2026-08-20

---

## 0. 文档使用方式

这份文档不是“愿景清单”，而是 V1 的实施边界。使用 Codex 开发时，应把本文件视为项目的主约束：先实现必须功能，通过验收后再进入增强项。任何新想法如果没有被列入 V1，默认进入 Later，不应在当前阶段顺手实现。

V1 只证明三件事：

1. **管资料：** 至少达到当前用思源笔记整理课程的下限，并更适合真实 Office/PDF 文件。
2. **找资料：** 能从课程、素材和外部资料中快速搜到文件及其内部相关文本。
3. **AI 备课：** 在当前课次中，选择参考资料后，用老师自己的 AI API Key 生成可审阅、可修改、可保存的备课草稿。

如果这三件事做得好，V1 就成功；其他功能不能反过来拖慢这三件事。

## 0.1 本次 R3 更新重点

本版在不扩大产品范围的前提下，继续保留 R2 的简化方向，并针对三项最可能影响 V1 实际可用性的工程风险做加固：中文/数学混合搜索、Office/WPS 修改后的刷新核对、search.db 重建体验。新增/强化的内容包括：

- 中文全文搜索不再只依赖单一 FTS5 tokenizer：增加 `SearchNormalizer`，并明确 trigram → 应用层 TokenExtractor → Later 轻量分词/搜索引擎的三级 Plan B；
- Phase 0.5 中文搜索 Spike 必须覆盖数学符号、题号和中英混排，例如 `|x|`、`∠ABC`、`△ABC`、`AMC8`、`P16`；
- 文档解析、批量 Hash、索引构建继续放入后台 Worker，避免阻塞 Electron Main；
- managed 文件的物理目录与左侧课程树彻底解耦；
- Office/WPS 修改以“启动/焦点返回/重新打开/手动刷新核对 + 文件稳定检测 + Hash 去重”保证最终一致；watcher 只作为可选加速器标记 dirty，不能成为唯一变化来源，也不得直接触发解析；
- search.db 重建/首次索引必须有可见且可恢复的进度，允许老师在部分文件完成时立即搜索已就绪内容；
- 文档解析统一经过 `DocumentParser` Adapter，第三方解析库只作为可替换实现；
- AI 备课增加极简 `ContextBuilder`，控制 token、去重并保留来源；
- API Key 使用 OS-backed secure storage（Electron 可优先使用 `safeStorage`）；
- 完整备份必须使用一致性 SQLite 快照，并带轻量 `manifest.json`；
- Phase 0.5 继续验证文档解析、中文搜索、Office 修改后的刷新核对和强杀恢复。

仍然明确不加入 LangChain、向量数据库、OCR、Agent Framework、课程版本继承、文件去重等 V1 外能力。

## 0.2 2026-08-20 Lean V1 范围裁决（冲突时优先）

产品负责人确认：T01–T08 的骨架和 Spike 成果继续有效；原 T09–T42 实施链退役，后续唯一有效的实施计划是 `implementation-tasks/lean-tasks/` 中的 L01–L12。本节、`implementation-tasks/LEAN_V1_DECISIONS.md` 与活动 Lxx 任务在发生冲突时，优先于本文后续保留的旧详细设计。旧章节继续作为背景和 Later 参考，不得据此扩大 Lean V1 验收或将任务标为阻塞。

Lean V1 仍只证明“管资料、找资料、AI 备课”三条核心流程，并采用以下生产裁决：

- 只管理导入工作台的 managed 副本；external roots 扫描、重定位和长期同步进入 Later；
- 文件一致性由启动、焦点返回、重新打开和手动刷新核对保证；生产 watcher 不是 V1 必需项；
- 搜索使用 `search.db`、SearchNormalizer、一个统一 Parser Adapter 和一个顺序 Worker；内存任务可在重启后重新扫描，不要求持久化队列、精确进度/续传或 Worker 池；
- PDF、DOCX、PPTX、XLSX、MD、TXT 走同一生产解析里程碑；不拆成四套独立框架，不做 OCR；
- 老师选择有限资料后，可分别生成讲义、例题或作业并立即保存为普通草稿；失败整次重试，不建立 AI Workflow/步骤状态机或内容哈希清单；
- 备份只需在暂停业务写入的空闲态，使用 SQLite backup API 加 managed 文件复制并恢复到新空目录；在线并发备份、孤儿修复系统和攻击矩阵进入 Later；
- Windows 使用当前工具链中最简单可复现的安装包或便携包；自动更新、完整升级矩阵和签名发布进入 Later；
- 普通里程碑验证相关测试、typecheck 和 lint；只有 L04、L07、L10、L12 做全量 gate 与 Sol 审核，不再为第三方软件内部极端时序建立巨大矩阵。

不可放松的边界保持不变：Renderer 不直接访问 SQLite、Node、任意文件系统或秘密；managed 正式文件使用安全对象路径、临时文件和原子重命名；长解析/Hash/索引不阻塞 Electron Main；API Key 不明文落盘、不进日志或备份；AI 只生成草稿，绝不覆盖老师原资料。

实现成本高、自动化不完美或缺少非核心极端矩阵，不构成阻塞。此时必须采用上述简单替代或移入 Later。只有核心 happy path 无法实现、继续会造成资料损坏/路径越界/Key 泄漏、缺少核心必需权限/凭据，或需要改变产品方向时，才可停止并请求用户决定。

---

# 1. 产品定位

## 1.1 一句话定义

**教师工作台是一个面向教培老师的、本地优先的个人教学资料工作台：用树形结构保存课程、学生和素材，用全文搜索找资料，并在当前课次中调用 AI 完成备课。**

它不是教务系统，不是云盘，不是完整知识图谱系统，也不是套壳 ChatGPT。

## 1.2 目标用户

V1 目标用户是：

- 教培老师；
- 一台电脑上的单个老师；
- 主要在 Windows 上使用；
- 有大量 PPTX、DOCX、PDF、MD、XLSX、图片等长期资料；
- 课程既有班课，也有一对一；
- 希望保留自己过去的课、学生记录和资料；
- 会自己配置 OpenAI、Claude、Gemini 或其他兼容模型的 API Key；
- 不需要云端多人协作。

V1 不设计账号体系。一台工作台就是一个老师自己的工作空间。

## 1.3 当前痛点

现有资料可能分散在：

- 思源笔记；
- Windows 文件夹；
- Office/WPS 文档；
- 题库 SQLite；
- 各种下载资料目录；
- ChatGPT 或其他 AI 工具。

问题不在于“没有文件夹”，而在于：

- 课程树和真实文件分离；
- 过去讲过什么容易忘；
- 文件名记不住时难以找到；
- AI 备课需要手工反复挑资料、粘贴上下文；
- 一对一学生跨多个年级、多个阶段时，简单的“第一课、第二课”会无限增长；
- 同一套素材用于不同班时，实际内容又可能不同。

---

# 2. V1 总原则

## 2.1 Local-first

业务数据和托管文件都保存在老师本机。V1 不建设自己的云端后端。

外部 AI API 仅在老师主动运行 AI 功能时调用。普通资料管理和全文搜索不依赖外部大模型。

## 2.2 AI 关闭后软件仍然要好用

如果老师没有配置 API Key，仍然必须能：

- 管课程；
- 管一对一阶段；
- 管学生；
- 管素材；
- 添加和打开文件；
- 全文搜索。

AI 不是核心数据存储方式。

## 2.3 简单优先

V1 不做为了未来而提前复杂化的系统。需要明确拒绝：

- 课程继承与自动同步；
- Git 式课程版本合并；
- 文件级去重与二进制增量；
- 通用 Workflow Builder；
- 通用 Agent 框架；
- 知识图谱；
- 向量数据库；
- OCR；
- 多端实时同步。

## 2.4 文件安全优先于磁盘占用

如果一个 50 MB 的 PPT 复制到某节课可以换来更简单可靠的行为，那么 V1 选择复制。

原则：**宁可多占磁盘，也不能出现 A 班修改了资料，B 班或素材库也跟着变化。**

## 2.5 搜索负责事实，AI 负责生成和理解

V1 搜索由本地全文检索完成。AI 备课可以使用搜索结果，但不要让大模型替代搜索引擎。

未来可以让 AI 调用统一 SearchService，但搜索能力必须独立存在。

## 2.6 V1 简单，但服务边界必须可扩展

V1 不提前实现 Agent、语义搜索、题库融合，但基础能力必须通过稳定 Service 暴露：

- `NodeService` 管树；
- `FileService` 管真实文件；
- `SearchService` 管全文检索；
- `AIService` 只通过上述能力拿上下文；
- 未来 Agent 如果出现，只调用这些 Service，不复制一套隐藏逻辑。

目标是“**功能晚实现，接口早隔离**”，而不是“为了未来先把功能做出来”。

---

# 3. V1 范围

## 3.1 必须实现

### A. 树形资料管理

- 我的课程；
- 素材库；
- 学生；
- 班课；
- 一对一；
- 一对一学习阶段；
- 课次；
- 普通文件夹；
- 简单备注；
- 拖拽/移动/排序；
- 回收站或软删除。

### B. 文件管理

- 外部资料目录接入；
- 工作台托管文件；
- 文件复制到课次；
- 用系统默认程序 / Office / WPS 打开；
- 外部资料默认只读；
- 课次自己的文件允许在 Office/WPS 中修改；
- 修改后自动检测并更新索引。

### C. 文档全文搜索

V1 支持正文文本提取和搜索：

- PDF（有文本层）；
- DOCX；
- PPTX；
- Markdown；
- XLSX；
- TXT（实现成本低，可顺带支持）。

图片只按文件名和元数据搜索，不做 OCR。扫描 PDF、整页图片型 PPT 等无法提取正文时，必须标记为“无可提取文本”或“解析失败”，不要让老师误以为已经全文可搜。

### D. AI 备课

固定工作流：

1. 当前课次；
2. 老师填写本次特殊要求；
3. 工作台搜索并展示候选资料；
4. 老师勾选资料或片段；
5. AI 生成讲义草稿；
6. 老师审核/修改；
7. AI 生成例题；
8. AI 生成作业；
9. 保存到当前课次。

V1 AI 输出以工作台内部 Markdown/文本为主，不生成 PPT。

### E. 备份恢复

- 完整备份工作台；
- 从完整备份恢复；
- 备份包含数据库、托管文件、AI 生成内容、老师配置；
- 不包含外部公共资料原件；
- 不包含搜索索引；
- 不包含 API Key。

## 3.2 V1 明确不做

以下功能全部进入 Later：

- 云端服务；
- 账号登录；
- 多人协作；
- Mac 正式适配；
- 鸿蒙正式适配；
- 向量/语义搜索；
- 本地 Embedding 模型；
- OCR；
- 图片题目识别；
- 扫描 PDF OCR；
- 学生 AI 长期画像；
- 知识树；
- 教材章节体系；
- 课程模板继承；
- 自动同步多个班的课程内容；
- 课程版本合并；
- 文件去重；
- 二进制增量存储；
- 多设备实时同步；
- AI 生成 PPT；
- AI 修改 PPT；
- 高质量 DOCX 自动排版；
- Workflow Builder；
- 通用自主 Agent；
- 老格式 DOC/PPT/XLS；
- 局部课程包分享；
- 校宝双向同步；
- 题库整体合并进工作台。

---

# 4. 信息架构与主要界面

## 4.1 左侧主导航

V1 建议固定为：

- 首页（可后做）；
- 我的课程；
- 素材库；
- 学生；
- 设置。

首页不是 V1 最优先。第一阶段只要左侧树 + 中间内容区 + 全局搜索可用即可。

## 4.2 我的课程

“我的课程”保存老师真正正在上或已经上过的班和一对一。

示例：

```text
我的课程
├── 五升六暑假A班
│   ├── 第一课
│   ├── 第二课
│   └── 第十一课
│       ├── 有理数的乘方.pptx
│       ├── 有理数混合运算.docx
│       ├── 作业.pdf
│       └── AI备课
│
├── 五升六暑假B班
│   └── ...
│
└── 张三一对一
    ├── 2026春 · 六年级下
    │   ├── 第一课
    │   └── 第二课
    └── 2028秋 · 八年级上
        ├── 第一课
        └── 第二课
```

## 4.3 一对一学习阶段

一对一不能从六年级到九年级一直累积“第一课、第二课……”。

必须允许阶段：

- `2026春 · 六年级下`；
- `2026暑假 · 小升初`；
- `2028秋 · 八年级上`。

学生中断几年再回来时，直接创建新阶段，不需要补中间历史。

阶段只是轻量的树节点，不是复杂教务学期系统。可以额外保存：

- 年级；
- 学期/阶段名；
- 学科；
- 开始日期；
- 结束日期（可空）；
- 状态（进行中/已结束）。

名字可由系统生成默认值，但老师可以自由修改。

## 4.4 素材库

素材库必须是自由树，不把分类方法写死。

允许老师按任何方式组织：

```text
素材库
├── 按年级
│   ├── 七年级
│   └── 八年级
├── 按专题
│   ├── 函数
│   └── 几何
├── 好PPT
├── 好讲义
└── 临时资料
```

也允许完全不同的组织方式。V1 不要求先选择年级、教材、章节、知识点才能添加素材。

素材可以是：

- 完整教学单元；
- 单独 PPT；
- 单独讲义；
- PDF；
- 题组；
- 知识点补充；
- 其他普通资料。

未来如果需要，可增加标签、收藏、智能筛选，但 V1 不做。

## 4.5 学生

学生 V1 是“电子学生文件夹 + 简单记录”。

学生页面至少显示：

- 姓名；
- 当前关联课程；
- 历史关联课程；
- 记录；
- 附件。

例如：

```text
张三

当前课程
- 张三一对一 / 2028秋 · 八上
- 五升六暑假A班

记录
- 8月10日：本节计算情况较好
- 8月15日：阶段测试 82 分

附件
- 测试照片.jpg
- 成绩单.pdf
```

学生页面不复制课程数据，只通过链接展示课程和阶段。

---

# 5. 核心数据模型：极简版

V1 业务数据库建议只保留少量核心表。

## 5.1 workspace.db

建议核心表：

1. `nodes`：所有树形节点；
2. `files`：真实文件记录；
3. `links`：跨树关系；
4. `external_roots`：外部资料根目录；
5. `ai_runs`：AI 备课任务状态；
6. `settings`：普通设置。

搜索单独使用 `search.db`，可随时删除重建。

## 5.2 nodes

`nodes` 表负责“东西放在哪里”。

建议字段：

```text
id                UUIDv7 / TEXT PRIMARY KEY
parent_id         父节点，可空
kind              节点类型
title             标题
body_md           简单备注或 AI 文本内容
sort_order        同级排序
meta_json         低频扩展元数据
created_at
updated_at
deleted_at        软删除
```

V1 `kind` 尽量少：

```text
folder
course
period
lesson
student
note
reusable_unit
```

解释：

- `course`：五升六暑假A班、张三一对一；
- `period`：一对一的某个阶段；
- `lesson`：第一课、第十一课；
- `student`：张三；
- `note`：课后反馈、AI 讲义草稿；
- `folder`：普通目录；
- `reusable_unit`：素材库中的完整教学单元。

不要继续增加大量类型。未来复杂能力通过附加表扩展。

### meta_json 使用原则

只放低频、非核心查询数据。例如 `period` 可以保存：

```json
{
  "grade": "八年级",
  "term": "秋",
  "subject": "数学",
  "status": "active",
  "startDate": "2028-09-01"
}
```

如果某字段以后变成高频过滤/排序条件，再通过数据库 Migration 升格为正式列。

## 5.3 files

`files` 负责“真实文件是什么”。

建议字段：

```text
id
parent_node_id
name
extension
storage_mode       external / managed
external_root_id   外部文件使用
relative_path      外部文件相对路径
managed_storage_key 工作台托管文件内部存储键（与课程树路径无关）
content_hash
size_bytes
modified_at
origin_file_id     从哪份素材/外部资料复制而来，可空
status             available / missing / error
created_at
updated_at
deleted_at
```

### 规则

- 树节点和文件记录分开；
- 文件显示在哪个课次/目录由 `parent_node_id` 决定；
- 文件内容不塞进 SQLite BLOB；
- 真实文件保存在文件系统。
- `parent_node_id` 只决定逻辑位置；拖动/重命名课程树不能触发真实文件搬家。

## 5.4 external_roots

用于管理外部资料目录。

```text
id
name
path
enabled
last_scan_at
created_at
updated_at
```

外部文件保存相对路径而非整条绝对路径。

例如：

```text
external_root.path = D:\数学教学资料
file.relative_path = 七年级\函数\一次函数.pptx
```

如果整个资料库搬到 `E:\教学资料`，只修改 external_root.path，不需要更新 5000 条记录。

## 5.5 links

树表示位置关系，links 表示关联关系。

建议字段：

```text
id
from_node_id
to_node_id
relation_type
created_at
deleted_at
```

例子：

```text
张三 -- enrolled_in --> 五升六暑假A班
张三 -- one_to_one --> 张三一对一
```

硬规则：

- “在哪里”用 `parent_id`；
- “和谁有关”用 `links`；
- 不要用 links 代替正常树结构。

## 5.6 ai_runs

AI 备课状态表只做任务恢复，不做通用工作流引擎。

建议字段：

```text
id
lesson_node_id
status             running / waiting_user / completed / failed / cancelled
current_step
request_text
state_json
created_at
updated_at
finished_at
error_message
```

`state_json` 可保存：

- 老师本次特殊要求；
- 已选文件 ID；
- 已选片段；
- 当前步骤临时状态。

## 5.7 settings

只保存普通设置，例如：

- 默认 AI Provider；
- 模型名称；
- 工作台目录；
- 默认 Office 打开方式；
- 备课 Prompt Override。

API Key 不得明文保存进 settings。

---

# 6. 文件系统设计

## 6.1 程序和用户数据必须彻底分离

程序可以安装到：

```text
C:\Program Files\TeacherWorkbench\
```

用户工作区放到：

```text
TeacherWorkspace\
├── data\
│   └── workspace.db
├── files\
│   └── objects\
│       └── <file-uuid>\原文件名.ext
├── search\
│   └── search.db
├── cache\
│   └── 预览/缩略图
└── backups\
```

软件升级或卸载不能自动删除工作区。

managed 文件的物理路径必须与课程树解耦。例如老师把“第十一课”拖到另一个阶段，程序只更新 `nodes.parent_id`，不能因为逻辑移动而重命名或移动 PPT/DOCX。推荐物理布局：

```text
files/objects/<file-uuid>/<sanitized-original-name.ext>
```

这样可以避免长路径、频繁移动、大量 rename，以及“改课程名导致文件路径全变”的风险。

## 6.2 external 与 managed

### external

- 指向老师已有文件；
- 工作台不移动；
- 默认只读；
- 不进入完整备份；
- 文件消失时标记 missing，而不是删除数据库记录。

### managed

- 工作台自己托管；
- 属于具体课次、学生附件、工作台素材、AI 生成内容；
- 可以通过 Office/WPS 修改；
- 必须进入完整备份。

## 6.3 素材/外部资料加入本课

V1 使用“真实复制”，不做写时复制和去重。

流程：

1. 老师在某课次点“从素材库添加”或“加入本节课”；
2. 程序为新业务文件生成 `file_id`，复制到 `files/objects/<file-id>/`；
3. 复制完成后创建新的 `files` 记录，并由 `parent_node_id` 指向当前课次；
4. `origin_file_id` 记录来源；
5. 新文件从此完全独立。

如果复制中途失败或软件退出，不能留下半个正式文件。

推荐实现：

```text
复制到 xxx.tmp
→ 完成
→ 校验
→ 原子重命名为正式文件
→ 写数据库事务
```

失败时清理 `.tmp`。启动时也应做轻量清理：移除明显过期的 `.tmp`，并记录/提示没有数据库记录的孤儿 managed 文件。V1 不必实现完整 `file_ops` 事务日志；如果后续文件操作明显复杂化，再加入日志式恢复。

## 6.4 Office/WPS 编辑

工作台不做 Office 编辑器。

对于 managed 文件：

1. 老师点击“打开”；
2. 工作台调用系统默认程序、PowerPoint、Word、Excel 或 WPS；
3. 老师在外部程序中编辑并保存；
4. 老师返回工作台、重新打开该文件或点击“刷新资料”时，工作台对该文件执行刷新核对；启动工作区时也在后台做轻量核对；
5. 文件稳定且可读取后重新计算 Hash；
6. Hash 变化时合并为该文件唯一的后台解析/索引任务；
7. 页面显示“索引更新中/已更新”，完成后搜索结果使用新内容。

如果文件本来来自素材库，由于加入本课时已复制，因此任何修改都不会影响原素材。

正确性不得依赖 Office/WPS 是否产生了某一种保存事件。权威触发点是：工作区启动后台核对、工作台从它启动的外部程序返回焦点、重新打开文件和手动刷新。启动/焦点核对先比较轻量 `size + modified_at`，需要时再在后台做稳定读取与 Hash；重新打开和手动刷新可直接强制核对目标文件。若文件仍在写入或暂时不可读，保留待核对状态并有界重试，不读取半文件，也不把中间态当永久失败。

可以使用 Chokidar 监听 managed 目录来缩短更新时间，但 watcher 只是加速器：原始事件只能标记 `dirty`，不得立即计算 Hash 或解析正文；漏报时必须由上述刷新核对恢复。对同一文件的 watcher、焦点和手动刷新触发统一合并，再检查稳定性和 Hash。Hash 未变化不建立任务；Hash 变化时同一 file_id 同时最多一个待执行/执行中的索引任务。不要把固定等待 2～3 秒写成正确性规则。

## 6.5 外部文件移动

V1 不需要复杂操作系统级 File ID 追踪，但应做到：

- 外部根目录整体迁移：修改 external_root.path；
- 单个文件找不到：标记 missing；
- 可提供“重新定位文件”；
- 内容 Hash 可帮助判断新文件是否为原文件。

---

# 7. 搜索系统

## 7.1 V1 搜索边界

V1 只做确定性的全文搜索，不做向量语义搜索。

必须搜索：

- 树节点标题；
- 文件名；
- PDF 文本层；
- DOCX 正文；
- PPTX 每张幻灯片文字；
- MD；
- XLSX 单元格文本；
- AI 生成的 note/body_md。

不搜索：

- 图片中的文字；
- 扫描 PDF 图片内容；
- PPT 图片里的文字；
- 手写内容。

## 7.2 search.db

搜索库是派生数据，不能成为业务真相。

建议核心表：

```text
search_documents
- id
- source_type
- source_id
- content_hash
- indexed_at

search_chunks
- id
- document_id
- ordinal
- position_type
- position_value
- heading_path
- text_content
```

再使用 SQLite FTS5 建全文索引。

### 7.2.1 中文搜索策略必须先做 Spike，并准备明确 Plan B

不能默认 `FTS5 + unicode61` 就等于“中文全文搜索可用”，也不能把 `trigram` 当作唯一答案。教师资料中会大量出现中文、英文、题号和数学表达式混排，例如 `有理数`、`AMC8`、`P16`、`|x|`、`∠ABC`、`△ABC`、`x²`。R3 要求在大量 UI 开发前，用真实中文数学资料做搜索实验，并把搜索实现封装在 `SearchService` 内，避免业务层依赖某个 tokenizer。

V1 搜索采用三级降级策略：

1. **Level 1：FTS5 trigram + SearchNormalizer。** 对正文和查询做轻量规范化，例如英文统一大小写、全角/半角统一、常见数学 Unicode 形式规范化，同时保留原始正文用于展示。3 个及以上汉字的连续词优先验证 trigram；
2. **Level 2：应用层 TokenExtractor。** 如果 trigram 对短词、数学符号或中英混排召回不足，额外提取中文短词、英文/数字标识符、题号和可稳定识别的数学表达式，写入额外可搜索字段。标题/文件名精确匹配与短词 fallback 可以作为 V1 的补充；
3. **Level 3：Later。** 如果真实资料证明 Level 1 + Level 2 仍不足，再在 V1.1+ 评估轻量中文 tokenizer、ICU/分词扩展或更专业的本地搜索方案。不得因为 Spike 不理想就直接在 V1 引入 Elasticsearch、Meilisearch、向量数据库或大型 NLP 服务。

Phase 0.5 至少验证：`有理数`、`一元二次`、`函数`、`几何`、`圆`、`AMC8`、`P16`、`|x|`、`∠ABC`、`△ABC`、`x²` 等查询的召回和速度。最终 tokenizer/Normalizer/TokenExtractor 组合由真实数据决定，不在规格阶段拍死。

## 7.3 索引/解析状态与可见进度

搜索派生层必须能区分：

- `indexed`：已成功解析并可全文搜索；
- `no_text`：文件可读，但没有可提取正文（常见于扫描 PDF/纯图片 PPT）；
- `parse_failed`：解析失败；
- `pending`：等待处理；
- `processing`：当前正在处理；
- `stale`：源文件已修改，等待重建。

索引状态必须可持久化，不能只存在内存中。程序异常退出时，原先处于 `processing` 的文件在下次启动时重新转为 `pending`；已经 `indexed` 的文件不重复处理。即使整个 `search.db` 被删除，也可以从业务数据库和真实文件重新生成这套队列。

V1 必须有简单、持续可见的索引进度 UI，例如：

```text
资料索引：3821 / 5126 已就绪
正在处理：1
等待：1273
无可搜索文本：19
解析失败：12
```

老师只需要看到“已就绪 / 正在准备 / 无可搜索文本 / 解析失败”等人类语言，不暴露 chunk、tokenizer 等技术词。索引未完成时，软件其他功能仍可使用，且已经完成索引的文件必须可以立即参与搜索，不要求等待全部资料处理结束。

## 7.4 内容位置

返回搜索结果时尽量保留位置：

- PPTX：`slide = 17`；
- PDF：`page = 32`；
- DOCX：标题路径 / 段落片段；
- MD：标题路径；
- XLSX：sheet + cell/range。

V1 不强求 Office 自动跳到精确位置。最重要的是：

1. 找到正确文件；
2. 显示正确匹配片段/页码/幻灯片号；
3. 能一键打开原文件。

## 7.5 索引触发与任务合并

新增文件可以直接进入 `pending` 队列。对已经存在的 managed 文件，刷新核对与可选 watcher 统一采用以下流程：

```text
启动/焦点返回/重新打开/手动刷新，或 watcher event
→ 标记 file_id 待核对/dirty
→ 合并同一文件的连续触发
→ 检查 size + modified_at 是否稳定
→ 确认文件可读取
→ 计算 content_hash
→ Hash 未变化：清除 dirty，不解析
→ Hash 变化：创建/合并为该 file_id 的唯一索引任务
→ 后台 Worker 重新解析
→ 受控 Index Writer 更新 search.db
```

同一 file_id 同时最多存在一个等待或执行中的索引任务。如果文件在任务执行期间再次收到刷新或 watcher 触发，只需要重新标记待核对，并在当前任务结束后再做一次稳定性/Hash 检查，禁止重复触发产生大量解析任务。Watcher 完全漏报时，下一次权威刷新核对仍必须发现变化。

## 7.6 全局搜索与当前范围

SearchService 统一提供：

```text
search({
  query,
  scopeNodeId?,
  fileTypes?,
  limit?
})
```

用途：

- 顶部全局搜索；
- 当前课程范围搜索；
- 当前学生范围搜索；
- 未来 AI Agent 调搜索。

不要把搜索逻辑写死在某个 React 页面组件里。

## 7.7 未来扩展

未来增加语义搜索时：

- 不改 `workspace.db`；
- 新增向量索引层；
- 仍由 SearchService 统一输出结果；
- AI 调用的也是 SearchService。

---

# 8. AI 备课系统

## 8.1 V1 只实现一个固定工作流

不要先抽象通用 Skill Framework。

V1 就做 `prepareLesson()` 这一条流程。

## 8.2 用户流程

进入某个 `lesson` 节点后：

1. 点击 `AI备课`；
2. 系统自动知道当前课次；
3. 老师填写特殊要求，例如“应用题多一点，难度偏高”；
4. SearchService 搜索当前课、素材库、外部资料；
5. 展示候选资料及片段；
6. 老师勾选参考资料；
7. 调文字模型生成讲义；
8. 老师审核/修改；
9. 生成例题；
10. 生成作业；
11. 保存为当前课次下的普通 note 节点。

结果示例：

```text
第十一课
├── 有理数.pptx
├── 原讲义.docx
└── AI备课
    ├── 讲义草稿
    ├── 例题
    └── 作业
```

这些内容直接保存在 `nodes.body_md` 即可。

## 8.3 ContextBuilder：V1 必须有，但保持极简

老师勾选资料后，不允许把整份 150 页 PDF、多个 PPT/DOCX 原样无脑塞给模型。增加一个独立 `ContextBuilder`：

```text
SearchService / 老师选择
        ↓
ContextBuilder
        ├── 只取选中或命中的片段
        ├── 去明显重复
        ├── 按相关度/当前课优先排序
        ├── 控制总 token/字符预算
        └── 保留来源 file_id + position + content_hash
        ↓
AIService
```

V1 不需要自动 RAG Agent，只需要保证送给模型的上下文有限、可追溯、可解释。

`ai_runs.state_json` 建议记录最少的来源清单：provider、model、prompt_version、selected file ids、选中位置、源文件 content_hash。这样以后能知道某份讲义基于哪些版本资料生成。

## 8.4 正常模式与快速模式

V1 可以先实现正常模式。若实现快速模式，原则是：

- 自动接受候选资料；
- 连续运行各步骤；
- 结果仍保存为草稿；
- 不直接改原课程资料。

## 8.5 Prompt 配置

V1 普通老师无需看到复杂参数。

设置中可以提供：

- 文字模型 Provider；
- 文字模型名称；
- 一个“备课 Prompt 高级设置”；
- `恢复默认`。

不做：

- 每一步单独模型；
- Temperature 调参面板；
- 节点拖拽；
- Workflow Builder。

## 8.6 API Key 安全

API Key：

- 不明文写入 SQLite；
- 使用 OS-backed secure storage；Electron V1 优先评估/使用 `safeStorage`；
- UI 只显示“已配置”；
- 支持替换/删除/测试；
- 备份时不导出。

---

# 9. 长任务、退出与崩溃恢复

V1 必须按“任务可以被中断”设计，但不追求 token 级断点续传。

## 9.1 普通搜索

关软件：直接取消。下次重新搜，无数据风险。

## 9.2 扫描/建立全文索引

必须做到**文件级恢复 + 可见进度 + 部分可用**。

例如 5000 个文件：

- 前 3217 个已经完成；
- 第 3218 个处理中时退出；
- 下次启动把第 3218 个重新放回 `pending`，只继续未完成/过期文件；
- 已完成的 3217 个文件立即可搜索；
- 第 3218 个可以整份重做，不需要从文件内部的 47% 继续。

索引进度必须持久化并在 UI 中持续可见。老师关闭再打开软件后，进度不能无故回到 0；单个文件解析失败不能阻塞整个队列。首次索引或全量重建可能耗时较长，但软件必须保持可操作，老师可以继续浏览课程、打开文件，并搜索已经索引完成的内容。

## 9.3 AI 备课

必须做到**步骤级恢复**。

例如：

```text
✓ 已选资料
✓ 已填写要求
✓ 讲义已完成
⚠ 例题生成中断
○ 作业未开始
```

下次打开：

- 已完成步骤保留；
- 中断中的“例题”从头重做；
- 不重新生成讲义；
- 当前正在进行的外部 API 调用可能已经计费且结果丢失，这是可接受限制。

不做：

- token 级恢复；
- 软件关闭后在云端继续任务；
- 自建后台任务服务器。

## 9.4 文件复制

不做断点续复制。

中途退出：删除/忽略 `.tmp`，下次整份重新复制。

## 9.5 SQLite 写入

关键写操作必须使用 Transaction。

原则：

```text
要么完整提交
要么完整回滚
```

不要出现半个 AI 结果、半条课程关系。

## 9.6 正常退出提示

如果存在运行中任务，正常点击退出时可以提示：

```text
还有任务正在运行：
- 正在建立索引
- AI备课：正在生成例题

[等待完成]
[退出，稍后继续]
```

但数据安全不能依赖这个弹窗。任务管理器强杀/断电也不能损坏核心数据。

---

# 10. 学生模块详细规则

## 10.1 V1 目标

学生模块只做：

- 学生节点；
- 简单记录；
- 附件；
- 课程链接；
- 一对一阶段链接。

## 10.2 不要求老师录逐题数据

现实中纸质作业和课堂练习无法要求老师逐题上传或录入。

因此 V1 不做“每道题做对/做错”的结构化追踪。

如果以后系统自然产生结构化测试数据，再利用即可。

## 10.3 记录

记录可以是普通 note：

- “今天计算错误比较多”；
- “阶段测试 82 分”；
- “本节状态不错”；
- “家长反馈……”等。

可后续扩展为结构化记录表，但 V1 不做。

## 10.4 附件

学生可以上传：

- 图片；
- PDF；
- DOCX；
- 其他普通附件。

V1 只存、看、打开，不做图片理解。

---

# 11. 外部资料与素材库的关系

这两类资料必须区分。

## 11.1 外部资料

含义：老师电脑上本来就存在的公共/参考资料。

特点：

- 不复制进工作台；
- 可索引；
- 默认只读；
- 删除索引不删除原文件；
- 不进入工作台完整备份。

## 11.2 工作台素材库

含义：老师主动保存的可复用内容。

来源可以是：

- 老师自己上传；
- 从某次实际课“保存到素材库”；
- 从外部资料复制进来。

特点：

- 工作台托管；
- 可通过 Office/WPS 修改；
- 进入完整备份；
- 组织方式完全自由。

## 11.3 实际课内容

一旦资料“加入本节课”，立即成为本节课自己的 managed 文件。

之后：

- 原素材变化不会影响本课；
- 本课修改不会影响原素材；
- 另一班复制同素材也得到自己的文件。

这是 V1 文件安全的核心规则。

---

# 12. 备份与恢复

## 12.1 备份内容

完整备份应包含：

- `workspace.db`；
- 工作台 managed files；
- 学生附件；
- 工作台素材；
- 课程与课次；
- AI 生成文本；
- Prompt 配置；
- 普通 settings。

不包含：

- 外部公共资料原件；
- `search.db`；
- 缩略图/缓存；
- API Key。

## 12.2 备份一致性与 manifest

不能在 SQLite 正在写入时直接复制 `workspace.db` 文件。BackupService 必须使用 SQLite 的一致性快照方式（例如 backup API 或等价安全方案）生成备份数据库。

备份包至少包含一个轻量 `backup_manifest.json`：

```json
{
  "backupVersion": 1,
  "schemaVersion": 1,
  "workspaceId": "...",
  "createdAt": "..."
}
```

V1 不要求对几十 GB 的每一个托管文件都计算 SHA-256 后再备份；全面 checksum 可以 Later。恢复时至少检查数据库可打开、schemaVersion 可迁移、manifest 与工作区身份合法、托管目录结构存在。

## 12.3 恢复

恢复流程：

1. 选择备份包；
2. 解包到**新的/空工作区**，不要直接覆盖当前工作区；
3. 校验 manifest 与 workspace.db；
4. 校验托管目录及关键文件可访问；
5. 完成数据库 Migration（若需要）；
6. 重新构建搜索索引；
7. 若 external_root 原路径失效，提示重新指定目录；
8. 老师重新配置 API Key；
9. 新工作区验证通过后才允许切换为当前工作区。

## 12.4 可选密码保护

如果实现成本可接受，可在 V1 后期提供：

- 普通备份；
- “使用密码保护”可选项。

这不是第一阶段阻塞项。

---

# 13. 技术架构建议

## 13.1 桌面技术

V1 推荐：

- Electron；
- React；
- TypeScript；
- SQLite；
- Node.js 文件系统能力；
- SQLite FTS5；
- 一个统一 AI Gateway。

原因：

- Windows 本地文件操作丰富；
- Office/WPS 外部打开方便；
- 文件监听、Hash、SQLite、备份开发成本低；
- Web UI 易做类似 ChatGPT/思源的左侧树 + 中间内容区；
- 未来 Mac 仍可复用大量代码。

V1 不把安装包大小作为主要优化目标。

## 13.2 进程边界

React Renderer 不直接访问 SQLite、任意文件系统或秘密 Key。

建议：

```text
React Renderer
    ↓ IPC
Electron Main
    ├── NodeService
    ├── FileService
    ├── LinkService
    ├── SearchService
    ├── AIService / AI Orchestration
    ├── BackupService
    ├── SettingsService
    └── Background Task Coordinator
           ↓
       worker_threads
       ├── Document Parse Worker
       ├── Hash Worker
       └── Index Build Worker
```

Electron Main 负责短事务、IPC 编排和业务状态，不执行长时间 CPU/IO 密集的文档解析、批量 Hash、首次大规模索引。V1 先用 Node 原生 `worker_threads`；只有任务队列明显复杂以后才考虑 Piscina 等 Worker Pool。

解析 Worker 尽量返回结构化结果，由受控的 Index Writer 写 `search.db`，不要让大量 Worker 无约束并发写 SQLite。

## 13.3 Service 设计

### NodeService

负责：

- createNode；
- renameNode；
- moveNode；
- deleteNode；
- restoreNode；
- listChildren；
- reorderChildren。

### FileService

负责：

- registerExternalRoot；
- scanExternalRoot；
- addManagedFile；
- copyFileIntoNode；
- openFile；
- watchManagedFile；
- markMissing；
- recomputeHash。

### LinkService

负责：

- linkStudentToCourse；
- unlink；
- queryRelatedNodes。

### SearchService

负责：

- indexFile；
- removeFromIndex；
- search；
- rebuildIndex；
- reportIndexStatus。

未来 AI 也调用 SearchService。

### ContextBuilder

负责：

- 将老师选择的文件/片段整理为模型上下文；
- 控制上下文预算；
- 去重复；
- 保留 file_id、position、content_hash；
- 输出稳定的 source manifest。

### AIService

负责：

- 调用文本模型；
- 管理 prepareLesson 流程；
- 保存步骤状态；
- 处理超时/失败/重试。

### BackupService

负责：

- createBackup；
- validateBackup；
- restoreBackup。

## 13.4 第三方库候选（必须先 PoC，不视为硬绑定）

| 能力 | 候选 | V1 决策 |
|---|---|---|
| Electron 项目骨架 | electron-vite / 同类成熟模板 | 可用 |
| 左侧树 | react-arborist | 优先验证 Controlled Mode |
| SQLite | better-sqlite3 | 优先 |
| 文件监听 | chokidar | 优先验证 Office/WPS 保存行为 |
| 文档解析 | officeParser | 只放在 Adapter 后，Phase 0.5 真实资料验证 |
| AI Provider | 直接 Provider SDK 或轻量统一层 | 简单优先，不引入 LangChain |
| Worker Pool | Node worker_threads | V1 足够；Piscina Later |

选择第三方库时必须重新核对：许可证、Electron 打包兼容、Windows/WPS 实际稳定性、维护状态。业务数据库与搜索库不得依赖第三方库私有 AST/数据结构。

---

# 14. 文档解析策略

## 14.0 统一 DocumentParser Adapter

业务层只依赖自己的解析接口，不直接依赖某个第三方库：

```text
DocumentParser.parse(file)
→ DocumentParseResult
   - text
   - chunks[]
       - text
       - positionType
       - positionValue
       - heading
   - parseStatus
```

Phase 0.5 优先用 `officeParser` 等候选库跑真实资料 PoC。如果某种格式效果不好，可以单独替换该格式 parser，而不改变 `SearchService/search.db` 的数据模型。

## 14.1 PPTX

V1 目标：

- 提取每张幻灯片可读取文字；
- 保存 slide number；
- 搜索结果显示幻灯片号；
- 文件打开交给 PowerPoint/WPS。

不做：

- 自己完整渲染 PowerPoint；
- 动画；
- SmartArt 完整解析；
- AI 修改 PPT。

## 14.2 PDF

- 有文本层：提取每页文字；
- 保存 page number；
- 扫描 PDF 没文字层时，V1 不 OCR；
- 可用内置 PDF Viewer 或系统打开。

## 14.3 DOCX

- 提取标题、段落、表格文字；
- 尽量保存 heading path；
- 搜索结果返回匹配片段；
- 编辑交给 Word/WPS。

## 14.4 XLSX

- 提取 sheet 名称和单元格文本；
- 可按 sheet 建 chunk；
- 复杂公式计算不是 V1 目标；
- 编辑交给 Excel/WPS。

## 14.5 Markdown

- 直接读取文本；
- 可保存标题层级；
- 可在工作台内显示和编辑简单 Markdown。

## 14.6 图片

- 显示缩略图/预览；
- 保存文件名、时间、备注；
- 不 OCR；
- 不自动生成图片描述；
- 对无正文的图片/扫描件返回 `no_text`，而不是伪装成已全文索引。

---

# 15. UI 行为要求

## 15.1 树

必须支持：

- 展开/折叠；
- 新建子节点；
- 重命名；
- 删除；
- 拖拽移动；
- 同级排序；
- 右键菜单或 `...` 菜单。

V1 不追求复杂块编辑器。

## 15.2 课次页面

进入一节课后，中间区域建议包含：

- 标题；
- 日期（可选）；
- 简单备注；
- 本节资料；
- `+ 从素材库添加`；
- `+ 从外部资料添加`；
- `AI备课`。

文件行至少提供：

- 打开；
- 在文件夹中显示；
- 删除/移除；
- 搜索命中时显示匹配片段。

## 15.3 素材页面

- 自由树；
- 文件拖入/导入；
- 从实际课“保存到素材库”；
- 加入当前课次时复制。

## 15.4 学生页面

- 当前课程；
- 历史课程；
- 简单记录；
- 附件。

## 15.5 搜索页面

全局顶部搜索框。

结果按类型展示：

- 节点；
- 文件；
- 文档内部匹配。

每个文件结果显示：

- 文件名；
- 所属树路径；
- 匹配片段；
- PPT页/幻灯片、PDF页、DOCX章节等位置；
- 打开按钮。

---

# 16. 任务恢复与一致性硬规则

所有未来新增功能都遵守：

1. **业务写入必须事务化。**
2. **长任务必须可判断完成/未完成。**
3. **索引是缓存，可重建。**
4. **文件复制先写临时文件，完成后原子重命名。**
5. **AI 每完成一步就保存。**
6. **正在执行的一步崩溃后允许整步重做。**
7. **核心课程/学生资料不能因为任务中断损坏。**

---

# 17. 题库的 V1 关系

现有题库继续作为独立 SQLite 项目。

V1 工作台不迁移、不重建题库。

如果 AI 备课需要题库，可后续增加一个最薄的 Adapter：

```text
QuestionBankService
- searchQuestions(filters)
- getQuestion(id)
```

优先使用题库原有结构化字段筛选，不把题库改成向量库。

题库整体合并进入工作台放到后续版本。

---

# 18. 项目目录建议

```text
src/
├── main/
│   ├── ipc/
│   ├── services/
│   │   ├── node-service.ts
│   │   ├── file-service.ts
│   │   ├── link-service.ts
│   │   ├── search-service.ts
│   │   ├── ai-service.ts
│   │   ├── context-builder.ts
│   │   ├── backup-service.ts
│   │   └── settings-service.ts
│   ├── db/
│   │   ├── connection.ts
│   │   ├── migrations/
│   │   └── repositories/
│   ├── parsers/
│   │   ├── document-parser.ts
│   │   ├── office-parser-adapter.ts
│   │   └── fallbacks/
│   ├── workers/
│   │   ├── parse-worker.ts
│   │   ├── hash-worker.ts
│   │   └── index-worker.ts
│   └── tasks/
│       ├── index-queue.ts
│       └── recovery.ts
│
├── renderer/
│   ├── app/
│   ├── components/
│   │   ├── SidebarTree/
│   │   ├── FileList/
│   │   ├── SearchBox/
│   │   └── AiPrep/
│   ├── pages/
│   │   ├── CoursePage/
│   │   ├── LessonPage/
│   │   ├── StudentPage/
│   │   ├── ReusablePage/
│   │   ├── SearchPage/
│   │   └── SettingsPage/
│   └── api/
│       └── ipc-client.ts
│
└── shared/
    ├── types/
    ├── schemas/
    └── constants/
```

目录可以调整，但原则不要变：UI、业务服务、数据库、解析器分层。

---

# 19. 开发阶段拆分

## Phase 0：项目骨架

目标：桌面应用能稳定运行，业务数据目录独立。

任务：

- Electron + React + TypeScript；
- IPC 基础；
- SQLite 初始化；
- Migration 机制；
- workspace 数据目录；
- 日志系统；
- 基础错误边界。

完成标准：

- 能启动/退出；
- 数据库能创建；
- 重新安装/升级程序不删除工作区。

## Phase 0.5：技术 Spike（在大量 UI 代码前完成）

目标：用真实资料消除四个最大技术不确定性，Spike 代码可以丑，但结果必须记录。

### Spike A：Document Parser

从真实资料中抽样 30～100 份：PowerPoint/WPS PPTX、DOCX、文本 PDF、扫描 PDF、XLSX、数学公式/表格/图片较多的文件。验证：

- 能否提取中文；
- PPT slide、PDF page、XLSX sheet 等位置是否稳定；
- 数学公式/表格降级成什么；
- 大文件耗时和内存；
- `officeParser` 是否足够作为主 Adapter。

### Spike B：Chinese Search

构造至少上万 chunk，覆盖中文短词、连续中文、中英混排和数学表达式。至少测试：`有理数`、`一元二次`、`函数`、`几何`、`圆`、`AMC8`、`P16`、`|x|`、`∠ABC`、`△ABC`、`x²`。比较 FTS5 `trigram`、`SearchNormalizer`、短词 fallback 和应用层 `TokenExtractor` 的召回与速度，并明确 Level 1 / Level 2 的采用条件。如果两级都不能满足真实资料，再记录为 V1.1 的搜索引擎/分词器评估项，而不是在 V1 临时引入大型搜索系统。

### Spike C：Office/WPS Refresh Reconciliation

使用实际支持的 Office/WPS 对 DOCX、PPTX、XLSX 做代表性普通保存和打开未改验证，确认最终文件可读；再用自动化探针验证即使完全没有 watcher 事件，启动、焦点返回、重新打开或手动刷新仍能通过稳定检测和 Hash 比较发现变化。一次新 Hash 最终只产生一次必要的重新解析/索引，Hash 未变不重建，并发触发合并。Chokidar 仅作为可选加速候选记录已观察到的事件特征；自动恢复、大文件保存和保存中退出不作为 V1 阻塞条件。

### Spike D：Crash Recovery

在复制文件、Hash、解析、建索引时强杀 Electron，重启后验证：业务文件不损坏、`.tmp` 可清理、索引任务可继续未完成文件。

完成标准：四项有记录、有结论；无法通过的候选库不进入正式代码。

## Phase 1：树 + 文件（最重要）

任务：

- nodes CRUD；
- 左侧树；
- 拖动/排序；
- 班课；
- 一对一；
- 一对一阶段；
- 学生；
- 素材库；
- files 表；
- managed 文件复制；
- external_root；
- 外部资料扫描；
- Office/WPS 打开；
- 文件变化监听；
- 回收站。

验收场景：

1. 创建“张三一对一”；
2. 创建“2026春·六下”和“2028秋·八上”；
3. 每个阶段建立若干课次；
4. 素材库有一个 PPT；
5. 把 PPT 加入 A 班；
6. 用 PowerPoint 修改 A 班 PPT；
7. 素材库原文件必须完全不变；
8. B 班复制同一素材后也不受 A 班影响；
9. 删除课次后可以恢复。

Phase 1 没通过，不进入搜索。

## Phase 2：全文搜索

任务：

- search.db；
- FTS5 中文策略按 Phase 0.5 结论落地；
- `SearchNormalizer`，必要时增加应用层 `TokenExtractor`；
- 后台解析/Hash/Index Worker；
- 解析状态（已就绪/无文本/失败/等待/处理中）；
- 索引进度持久化与 UI 展示；
- PDF 解析；
- DOCX 解析；
- PPTX 解析；
- MD 解析；
- XLSX 解析；
- 搜索结果 UI；
- 刷新核对后的 stability/hash/coalescing 增量重建，以及可选 watcher dirty 加速；
- 索引任务队列；
- 索引恢复；
- 索引未完成时允许搜索已就绪文件。

验收场景：

- 输入“有理数混合运算”；
- 能找到正确课程/素材；
- 能返回 PPT 相关幻灯片号；
- 能返回 PDF 页码；
- 能返回 DOCX 匹配段落；
- 修改 PPT 后返回工作台或手动刷新，搜索结果能更新；
- 建索引到一半强杀程序，重启后不用从第一个文件重建；
- 重启后 UI 能显示接近退出前的已完成数量，`processing` 文件重新进入等待队列；
- watcher 漏报时刷新仍能恢复一致；大量 watcher/刷新触发不会为同一文件产生重复索引风暴；
- 索引尚未完成时，已经就绪的文件可以正常搜索。

## Phase 3：AI 备课

任务：

- Provider/模型设置；
- Windows 安全保存 API Key；
- AI Gateway；
- ContextBuilder + source manifest；
- ai_runs；
- 当前课上下文；
- SearchService 候选资料；
- 资料确认页；
- 讲义生成；
- 人工修改；
- 例题生成；
- 作业生成；
- 结果保存为 note；
- 步骤级失败/重试/恢复。

验收场景：

- 进入第十一课；
- 输入“应用题多一点，偏难”；
- 选择 3 份历史资料；
- 生成讲义并修改；
- 生成例题和作业；
- 退出后重新打开，已完成步骤仍在；
- 在例题生成时关软件，重启后只需重做例题步骤。

## Phase 4：备份、安全、正式自用

任务：

- SQLite 一致性快照；
- 完整备份 + 简单 manifest；
- 完整恢复到新工作区；
- 外部目录重新定位；
- 崩溃恢复检查；
- `.tmp` 清理；
- 数据完整性检查；
- 正式迁移自己的课程/学生资料。

验收场景：

- 在电脑 A 生成完整备份；
- 用一个空工作区恢复；
- 课程、学生、素材、托管文件全部存在；
- 搜索索引重新生成；
- API Key 需要重新配置；
- 外部资料不在备份里，但可以重新指定目录。

---

# 20. V1 三个最终验收目标

## 20.1 管资料

目标：至少不低于当前思源笔记的课程树体验，并能管理真实 Office/PDF 文件。

验收：

- 一个跨六年级到八年级的一对一学生能够自然分阶段；
- 不会无限累积“第一课～第一百课”；
- 不同班级的资料互不影响；
- 素材库组织方式不被系统写死。

## 20.2 找资料

目标：几秒内找到“哪门课 → 哪一节 → 哪个文件 → 哪个内部位置”。

V1 不要求语义搜索，但确定关键词搜索必须可靠。

## 20.3 AI 备课

目标：比“文件夹 + 思源 + ChatGPT 反复复制粘贴”明显少切换、少粘贴。

必须做到：

- 当前课上下文自动带入；
- 资料由本地搜索提供；
- 老师确认来源；
- 结果可修改；
- 结果可保存；
- 中途失败不会让前面全部白做。

---

# 21. V1 风险排序

## 风险 1：范围重新膨胀

最危险。开发时看到“顺手可以做”时，默认写入 Later。

## 风险 2：文件所有权不清导致串数据

例如 A 班保存 PPT 后素材库也被改。这属于严重缺陷，必须有自动测试覆盖。

## 风险 3：文档解析或中文/数学混合搜索效果不稳定

尤其 PPTX/DOCX/PDF，以及中文短词、数学符号、题号和中英混排。优先保证“搜到正确文本和位置”，不要先追求完美预览；搜索层必须保留 `SearchNormalizer` / `TokenExtractor` 的 Plan B，而不是把业务逻辑绑死在单一 FTS5 tokenizer 上。

## 风险 4：AI 备课只是换皮 ChatGPT

如果老师仍然需要自己找文件、复制全文、重新交代当前课，那么 AI 工作台价值不成立。

## 风险 5：备份完成太晚

一旦正式把真实课程资料迁进去，完整备份恢复必须可用。

## 风险 6：Electron Main 被解析/Hash/索引阻塞

大文件和首次索引必须放后台 Worker。UI 在建立索引时仍应保持可操作。

## 风险 7：Office/WPS watcher 漏报或事件风暴导致索引不一致

Watcher 不能成为正确性的唯一来源，也不能直接触发解析。启动、焦点返回、重新打开或手动刷新必须能够通过稳定检测和 Hash 对比恢复一致；watcher 只提前标 dirty。所有触发都经过单文件任务合并和 Hash 去重，同一文件同时最多一个索引任务。

## 风险 8：首次索引/重建 search.db 像“卡死”

首次索引可能持续很久。必须持续展示已完成、处理中、等待、无文本和失败数量，并允许老师搜索已经完成的内容；重启后继续未完成文件而不是从 0 开始。

---

# 22. 必须写的自动测试/集成测试

至少覆盖以下场景：

1. `moveNode()` 不丢子节点；
2. 软删除节点后可以恢复；
3. 一对一阶段可以中断几年后新增阶段；
4. 素材 A 复制进 A 班得到独立文件；
5. A 班文件修改后素材 A Hash 不变；
6. A 班修改不影响 B 班；
7. 文件复制中断不留下正式半文件；
8. 外部文件 missing 时数据库不会删除该资料；
9. 外部 root 换路径后相对路径仍能正常解析；
10. 修改 managed PPTX 后，即使 watcher 漏报，焦点返回/重新打开/手动刷新也能触发重新索引；
11. 搜索索引可以全部删除并重建；
12. 索引进行中强杀程序，重启能继续未完成文件；
13. AI 讲义完成后断开网络，讲义仍然保存；
14. AI 例题步骤失败后，只重试例题；
15. 完整备份恢复后核心数据一致；
16. 备份中不存在 API Key；
17. 删除 search.db 后业务数据完全不受影响；
18. 拖动/重命名课程树不会移动 managed 文件的物理路径；
19. 中文搜索至少覆盖“有理数 / 一元二次 / 函数 / 几何 / 圆 / AMC8 / P16 / |x| / ∠ABC / △ABC / x²”的 Spike 用例；
20. `no_text` 与 `parse_failed` 能被区分并在 UI 显示；
21. 建索引时 Renderer/Main 不出现长时间假死；
22. 同一 managed Office 文件连续收到 watcher、焦点或手动刷新触发时，最终最多执行一次必要的重新解析/索引；
23. 索引进度在重启后能恢复，原 `processing` 文件重新进入等待，已完成文件不重复处理；
24. 索引未全部完成时，已 `indexed` 的文件仍可正常搜索；
25. ContextBuilder 不会把未选择的整份大文档无脑发给模型；
26. AI 生成结果保留 source manifest 与源文件 content_hash；
27. 备份数据库使用一致性快照，恢复到新工作区后可打开；
28. 正常备份包不包含 `search.db`、缓存和 API Key。

---

# 23. Codex 实现约束

使用 Codex 开发时，建议把以下规则写入仓库 README / AGENTS.md：

1. **不要擅自增加 V1 范围。**
2. **不要为了未来引入复杂抽象。**
3. **优先可读、可测试的代码。**
4. **Renderer 不直接访问 SQLite。**
5. **文件写入涉及最终路径时使用临时文件 + 原子重命名。**
6. **数据库核心写操作使用事务。**
7. **搜索数据必须可以无损删除重建。**
8. **所有 AI 输出默认作为草稿，不覆盖老师原资料。**
9. **任何素材加入实际课后都必须独立。**
10. **外部资料默认只读。**
11. **API Key 不得写入明文配置文件、日志、备份。**
12. **先写测试验证文件隔离，再继续做高级功能。**
13. **Phase 1 未完成，不提前开发向量、Agent、知识树等 Later 项。**
14. **managed 文件物理路径不得由课程树路径拼接。**
15. **解析/Hash/大批量索引不得长时间阻塞 Electron Main。**
16. **第三方文档解析库必须包在 `DocumentParser` Adapter 后。**
17. **中文搜索策略必须以 Phase 0.5 的真实数据结果为准，并保留 SearchNormalizer/TokenExtractor 的明确 Plan B。**
18. **不要引入 LangChain、通用 Agent Framework、向量数据库来解决 V1 问题。**

---

# 24. 推荐的第一批 Codex 任务

不要一次让 Codex “实现整个教师工作台”。按小任务提交。

## Task 1：初始化桌面项目

要求：

- Electron + React + TypeScript；
- Electron Main / Renderer 分离；
- 安全 IPC 示例；
- SQLite 可连接；
- 用户工作区可配置。

## Task 2：Phase 0.5 技术 Spike

要求：

- DocumentParser PoC（officeParser 仅作候选）；
- FTS5 中文 trigram/短词 fallback PoC；
- Chokidar/候选 watcher 的 Office/WPS 保存 PoC；
- 复制/解析/索引时强杀恢复 PoC；
- 输出一份 `docs/spike-results.md`，明确采用/不采用哪些方案。

## Task 3：数据库 Migration + nodes

要求：

- nodes 表；
- UUID；
- create/list/rename/move/delete/restore；
- Repository + Service；
- 单元测试。

## Task 4：左侧树 UI

要求：

- 展开折叠；
- 新建；
- 重命名；
- 删除；
- 移动/排序；
- 不直接访问 SQLite。

## Task 5：课程 / 一对一阶段

要求：

- course / period / lesson；
- 创建“张三一对一”；
- 新建多个不连续阶段；
- 每个阶段拥有自己的课次。

## Task 6：files + managed 文件

要求：

- files 表；
- 添加文件；
- 复制到 `files/objects/<file-uuid>/`，物理路径与树解耦；
- 临时文件 + 原子 rename；
- 打开文件；
- 根据 Spike 结论实现文件稳定监听。

## Task 7：素材库与复制

要求：

- 素材自由树；
- 素材加入课次是真复制；
- origin_file_id；
- A/B 班隔离测试。

## Task 8：external_roots

要求：

- 添加本地根目录；
- 扫描文件；
- 保存相对路径；
- missing 状态；
- 根目录重定位。

## Task 9：search.db + 一个格式

先只实现 Markdown/TXT 搜索，把 SearchService 跑通；中文 tokenizer/短词查询按 Phase 0.5 结论实现。

## Task 10：逐个增加 PDF/DOCX/PPTX/XLSX parser

统一走 `DocumentParser` Adapter；每增加一种格式都单独测试，不一次全写。

## Task 11：AI 备课最小闭环

先只做：

```text
课次 → 填要求 → 选资料 → 生成讲义 → 保存
```

跑通以后再加 ContextBuilder、来源清单、例题、作业、步骤级恢复。

## Task 12：备份恢复

在正式迁移真实教学资料之前完成；数据库必须使用一致性快照，并恢复到新工作区验证。

---

# 25. Later 路线图（不属于 V1）

V1 稳定后可以按真实痛点选择，不需要按顺序全部做。

## 25.1 语义搜索

- 本地 Embedding；
- 向量索引；
- 与 FTS/结构条件融合；
- AI 调 SearchService 多轮搜索。

## 25.2 学生 AI 分析

前提是自然积累了足够：

- 反馈；
- 成绩；
- 课次；
- 附件；
- 结构化测试数据。

不要求老师为 AI 额外逐题录数据。

## 25.3 知识点体系

未来建立：

- 教材章节树；
- 独立知识点树；
- 现有题库标签映射；
- 课次关联知识点。

## 25.4 题库融合

现有 SQLite 题库通过 Adapter 接入，再决定是否最终合并。

## 25.5 素材标签

当自由树无法满足“同一资料属于多个分类”时，再增加：

- tags；
- file_tags；
- node_tags。

## 25.6 AI 工具扩展

当“备课”稳定后再出现：

- 课后反馈；
- 学生分析；
- 资料总结；
- 试卷分析。

当稳定 Skill 数量明显增加后，才考虑真正的 Skill Framework。

## 25.7 PPT 能力

未来可能：

- 生成 PPT Agent Prompt；
- 外部专业 PPT Agent；
- AI 辅助修改副本。

不建议自己实现 PowerPoint 编辑器。

## 25.8 文件事务日志与深度备份校验

仅当 V1 真实使用中出现大量复杂文件操作，再增加：

- `file_ops` pending/committed journal；
- 启动补完/回滚；
- 全文件 SHA-256 manifest；
- 更完整的备份完整性验证。

这些不是 V1 的 P0 阻塞项。

## 25.9 跨平台

Windows V1 稳定后再验证 macOS；鸿蒙单独评估。不要在 V1 为三端兼容牺牲本地文件体验。

---

# 26. Definition of Done：V1 何时可以认为完成

以下条件全部满足，才算 V1 完成：

### 管资料

- [ ] 可以建立班课树；
- [ ] 可以建立一对一树；
- [ ] 一对一可以分多个不连续阶段；
- [ ] 可以建立自由素材树；
- [ ] 可以添加真实文件；
- [ ] 实际课资料与素材完全隔离；
- [ ] Office/WPS 修改后，返回工作台、重新打开或手动刷新能检测变化；
- [ ] 删除数据可以恢复。

### 找资料

- [ ] 文件名搜索可用；
- [ ] 节点标题搜索可用；
- [ ] PDF/DOCX/PPTX/MD/XLSX 正文可搜索；
- [ ] 搜索结果显示所属课程/路径；
- [ ] PPT/PDF 等尽量显示内容位置；
- [ ] search.db 可完全重建；
- [ ] 索引任务中断后能继续未完成文件；
- [ ] 中文短词、连续中文、中英混排和典型数学表达式通过真实 Spike 验收；
- [ ] 无文本层与解析失败可区分；
- [ ] watcher 漏报时刷新仍能恢复一致，连续保存或重复刷新不会产生重复索引风暴；
- [ ] 索引进度持续可见，重启后能够继续未完成文件；
- [ ] 索引未完成时已就绪文件仍可搜索；
- [ ] 大规模解析/索引时 UI 不被 Electron Main 长时间阻塞。

### AI 备课

- [ ] 可以配置文字模型和 API Key；
- [ ] API Key 安全保存；
- [ ] 当前课上下文自动带入；
- [ ] 可以选择参考资料；
- [ ] 可以生成讲义；
- [ ] 可以人工修改；
- [ ] 可以生成例题；
- [ ] 可以生成作业；
- [ ] 输出保存到当前课；
- [ ] 完成步骤不会因后续失败丢失；
- [ ] 中断步骤可以重做；
- [ ] ContextBuilder 控制上下文大小并保留来源；
- [ ] AI 结果可追溯到选中的 file_id/位置/content_hash。

### 安全

- [ ] 程序与工作区彻底分离；
- [ ] 核心数据库写操作使用事务；
- [ ] 文件复制中断不留半文件；
- [ ] 拖动/重命名课程树不改变 managed 文件物理路径；
- [ ] 外部资料不会被工作台误删；
- [ ] 备份使用 SQLite 一致性快照并带基础 manifest；
- [ ] 完整备份可创建；
- [ ] 新/空工作区可恢复；
- [ ] API Key 不进入备份；
- [ ] 删除 search.db 不影响任何核心资料。

---

# 27. Codex / Sol 使用策略（建议）

为了控制成本和避免过度工程化：

- 默认使用 Sol Medium 完成大多数 CRUD、UI、普通 Service、测试与集成；
- 数据库 Migration、Electron IPC 安全、文件一致性、中文搜索方案、复杂崩溃 Bug、大规模重构时使用 High；
- 纯样式、文案、小组件等机械任务可用 Low/Medium；
- 每个 Phase 完成后，可单独用 High 做一次“只审查、不扩功能”的代码审查。

不要让 Codex 一次“读取整个项目并继续完善”。任务应尽量限定文件与验收条件，例如：

```text
只实现 FileService.copyIntoNode()：
- managed 文件落到 files/objects/<uuid>/
- 使用 tmp + rename
- 不改变现有 UI
- 补隔离测试和强杀恢复测试
```

这样能显著减少无意义上下文重读和 V1 范围漂移。

# 28. 最后原则

如果开发过程中不确定某功能该不该做，用下面的判断顺序：

1. **它是否直接改善“管资料 / 找资料 / AI备课”之一？**
2. **它不做会不会阻止自己真实使用 V1？**
3. **它是否会明显增加数据库、文件系统或 AI 工作流复杂度？**
4. **能不能先用一个更笨但可靠的方式实现？**

例如：

- 文件去重：不能阻止自用 → Later；
- 语义搜索：全文搜索已经能用 → Later；
- PPT 生成：不影响备课文本闭环 → Later；
- 备份恢复：真实资料可能丢失 → V1 必须；
- 素材复制隔离：否则会串数据 → V1 必须；
- AI 步骤级保存：实现成本低且防止白做 → V1 应做。

最终开发目标不是一次做出一个“完美教师平台”，而是尽快做出一个自己每天愿意打开的工具。V1 稳定以后，未来的语义搜索、学生分析、知识树、题库融合、AI Agent 都有自然扩展位置，不需要现在提前支付复杂度成本。
